# local-finance — 지방재정을 시민이 들여다보는 곳

> 기록은 이 파일 한 곳에만 적는다. 새 문서를 만들고 싶어지면 만들 때가 아니라 걷어낼 때다.
> 판단과 결정은 맨 아래 「기록」 칸에, 데이터 소스의 상태는 `docs/지방재정_데이터소스_점검표.xlsx` 에.

**화면 → https://bada17.github.io/local-finance/** — **243곳 아무 데나 고를 수 있다.**
주소 끝에 `#4612000` 처럼 코드를 붙이면 그 자치단체로 바로 열린다.
저장소는 2026-09-21 에 공개로 돌렸다. 키는 들어 있지 않다.

## 이게 무엇인가 (2026-09-21 사용자 설명)

⭐ **기초단체 243곳의 재정을 한 판에 놓고 보여주는 곳이다.** 그런 판이 지금 세상에 없다.
**무엇을 이상하다고 할지는 그때마다 갈린다** — 기준을 미리 하나로 못 박지 말 것.

하는 일은 둘이다.

1. **무엇이 공개돼 있고 무엇이 안 돼 있는지** 가린다 (단체가 이어 오던 **투명성 지표**가 여기 붙는다).
2. 공개된 자료에서 **무엇이 문제이거나 이상한지** 본다.

⚠️ **투명성 지표는 중요하지만 「하나의 요소」다**(2026-09-21 사용자 정정).
사이트 전체를 투명성 지표로 짜지 말 것 — 그건 여러 축 가운데 하나다.

- **주체는 함께하는 시민행동이다.** 다른 단체와 같이 만들 수 있고 이걸 매개로 연대할 수 있지만,
  방향은 우리가 잡고 **우리가 지적한 것도 이야기한다.**
- **시민이 쓰는 기능도 있었으면 한다** — 들어와 보고 "재미있네" 하고, **의견을 남기고 제보도 하는**.
- **이미 있는 플랫폼의 빈자리** — 실제 데이터를 보여주지 않고, 시민 참여가 약하다. 우리는 그 둘을 한다.
- **1년 뒤 성공 = 회원이 늘고 이슈가 커지는 것.**
- ⛔ **교육은 아직 하지 않는다**(2026-09-21). 교육용 자료를 지레 만들지 말 것.
- ⏳ **11월 초 — 계획서를 쓸 만큼 준비돼 있어야 한다.**

참여연대 「열려라국회」의 재정판. **기초단체 243곳까지** 다룬다.
숫자만 늘어놓으면 지방재정365 짝퉁이 되니, **같은 숫자로 편을 드는 것**이 이 사이트가 할 일이다.

## 지금 가진 것

| 소스 | 종수 | 파일 |
|---|---|---|
| 지방재정365 | **146종 전부 열림** — 명세·필드·건수까지 | `docs/지방재정365_API코드_146종.xlsx` · `data/lofin365_146.json` |
| 열린재정 | 198종 (코드 전부, 87종 호출 확인) | `docs/열린재정_데이터목록_198종.xlsx` · `data/openfiscal_198.json` |
| 나라장터 | 계약·낙찰·입찰 | 계약정보 호출 확인 |
| 법정동코드 | 조인 키 | 호출 확인 |

**소스 18곳의 확인 상태는 점검표 파일에 있다.** 새 소스를 볼 때마다 그 파일을 갱신할 것.

`scripts/` 에 있는 것 — `spec_lofin.py`(명세 받기) · `probe_lofin.py`(되는지 눌러 보기) ·
`build_map.py`(지도 엑셀 굽기) · `fetch_lofin.py`(실제 수집). 앞의 셋은 지도를 만드는 쪽,
마지막 하나가 데이터를 받는 쪽이다. **지도 엑셀은 손으로 고치지 말 것** — 다시 구우면 덮어쓴다.

화면을 굽는 것은 일곱이다(하나는 노드다).

```
fetch_indicators.py   지표 28종을 243곳 전수로 받는다        → data/indicators/
build_model_data.py --all   자치단체별로 묶고 순위를 매긴다   → site/data/loc/<코드>.json + index.json
build_ongoing.py --all      진행 중인 사업(QWGJK)을 쪼갠다     → site/data/ongoing/<코드>.json
build_model_page.py         목록만 끼워 페이지를 굽는다        → site/index.html
build_transparency.py       공시 점검(세 켜)을 굽는다           → site/transparency.html
build_contracts_summary.py  계약 320만 건을 곳×연도로 센다      → data/contracts_summary.json
build_datamap.py            무엇을 말할 수 있고 무엇이 비었나    → site/datamap.html
```

자료를 받는 쪽에 **노드로 도는 것이 하나 있다** —
`node scripts/recheck_disclosure.js` 가 크롬(CDP)으로 243곳 재정공시 페이지를 다시 열어
`data/disclosure_dom.json` 을 만든다. 파이썬 `urllib` 로는 안 보이는 것을 잡는 자리다.

**페이지에 박히는 것은 243곳 목록(23KB)뿐이다.** 고른 곳의 파일만 화면이 그때 받아 온다 —
243곳을 통째로 담으면 2.3MB 라 첫 화면이 무거워진다.
**`site/index.html` 은 손대지 말 것** — `model.template.html` 을 고치고 다시 구울 것.

## 무슨 자료가 있나 — 화면으로 만들어 뒀다

**→ `site/catalog.html`** (화면 위 「무슨 자료가 있나」 단추). 지방재정365 146종을 한 판에 늘어놓고
**이름으로 찾을 수 있게** 했다. "재정자립도 자료가 있나"를 여기서 5초에 답한다(있다 — 결산·당초·최종 셋).
거르개 넷 — 전부 / 2024년 이후만 / 멈춘 것 / 아직 못 받는 것.

굽는 법: `python scripts/build_catalog_page.py` (지도 `data/lofin365_146.json` + 조사 `data/coverage.json`).

## 투명성 지표는 「올렸나」로 끝나지 않는다

⚠️ **2026-09-21 사용자 못박음 — "투명성 지표 하는 건 엄청 어려운 거야."**
> **"사실 다 공시가 되어 있어도 볼 수 없게 작게 만들거나 그런 식이면 그것도 문제가 돼."**

그래서 지표는 **있나/없나 한 칸으로 끝나면 안 된다.** 적어도 세 켜로 본다.

| 켜 | 묻는 것 | 우리가 잴 수 있는 것 |
|---|---|---|
| ① **있나** | 법이 시킨 것을 올렸나 | 공시 페이지의 파일 이름, API 제공 여부 |
| ② **닿을 수 있나** | 찾아갈 수 있나 | 링크가 살아 있나(404 여섯 곳 확인), 몇 번 눌러야 나오나 |
| ③ **쓸 수 있나** | 읽고 따져 볼 수 있나 | 엑셀·CSV 인가 그림 PDF 인가, 파일 이름이 뜻을 갖나(해시면 못 찾는다), 언제 올렸나 |

③이 핵심이다. **「올리긴 올렸다」로 빠져나가는 것을 잡는 칸**이다.
Open Budget Survey 도 이용가능성·적시성·포괄성을 따로 매긴다 — 단체가 그 조사에 참여했던 것이 이 사업의 뿌리다.

### → 화면으로 만들었다 (`site/transparency.html`)

**세 켜를 그대로 화면에 옮겼다.** 243곳 재정공시 주소를 전부 눌러 본 결과를 깔때기로 보여주고,
곳마다 한 줄씩 켜별 칸(링크·붙은 파일·이름·형식·기계가 읽나·60조 몇 가지)을 세운다.
줄을 누르면 붙은 파일 이름과 60조 16항목 대조가 펼쳐진다.

굽는 법: `python scripts/build_transparency.py`
(재료 — `data/disclosure.json` 243곳 실측 + `data/coverage.json` 146종 조사 + `site/data/loc/index.json`).

⚠️ **화면에 대놓고 적어 둔 것** — 이 판이 재는 것은 「공시했는가」가 아니라
**「자동 점검이 볼 수 있었는가」**다. 마크도 셋으로 갈랐다 — `○` 됐다 · `✕` 안 됐다 ·
**`?` 자동으로는 못 봤다(안 됐다가 아니다).** 해시 이름·자바스크립트 목록·SSL 은 전부 `?` 다.

## 지방재정법이 공개하라는 것 — 투명성 지표의 기준

**제60조 제1항**이 뼈대다. 예산·결산 확정 뒤 **2개월 안에** 주민에게 공시해야 하는 것 16가지.

| | 법이 공개하라는 것 | 지방재정365에 있나 |
|---|---|---|
| 1 | 세입·세출예산 운용상황(성과계획서·성과보고서 포함) | ✔ 예산·결산 계열 |
| 2 | 재무제표 | ✔ 재정상태표·통합재정운영표(2만 행 넘어 따로 받아야) |
| 3 | 채권관리 현황 | ✔ 채권현황 |
| 4 | 기금운용 현황 | ✔ 기금 계열 |
| 5 | 공유재산의 증감 및 현재액 | ✔ 재산·물품 계열 |
| 6 | 지역통합재정통계 | ✔ 11종 |
| 7 | 지방공기업·출자출연기관 경영정보 | △ 비율만. 기관별 알맹이는 **클린아이**에 있다 |
| 8 | 중기지방재정계획 | ✔ 재정운용계획 22종 |
| 9 | 성인지 예산서·결산서 | ✖ **안 보인다** |
| 10 | 예산편성기준별 운영 상황 | ✔ 주요경비 예산편성 4종 |
| 10의2 | **주민참여예산제도 운영현황·주민의견서** | ✖ **안 보인다**(주민e참여도 전국 통합본이 없다) |
| 11 | 재정운용상황개요서 | △ 확인 필요 |
| 12 | 재정건전화계획·이행현황 | △ 확인 필요 |
| 13 | 재정건전성관리계획·이행현황 | △ 확인 필요 |
| 14 | **투자심사·지방채·민간자본유치·보증채무 사업 현황** | △ 지방채·보증채무는 있고 **투자심사는 없다** |
| 15 | **지방보조금** 교부현황·성과평가·중요재산 변동·교부취소 | △ 편성현황만. 교부·성과평가는 **보탬e** 쪽 |
| 16 | 그 밖에 대통령령으로 정하는 것 | — |

다른 조항도 있다 — **제37조의4(주요 사업의 공개)** 투자심사·지방채 사업의 심사 결과와 추진상황,
**제29조의4(조정교부금 세부명세 공개)**, **제39조(주민참여예산)**, **제60조의2(통합공시)**.

⭐ **✖ 와 △ 가 곧 지표가 된다.** "법이 공개하라는데 API 로는 못 받는다"는 것 자체가 우리가 할 말이다.
다만 **자치단체 홈페이지에는 올라와 있는데 API 로만 안 오는 것**일 수 있다 — 표에 적기 전에 몇 곳을 눌러 봐야 한다.

법 본문은 `scripts/` 로 받지 않는다. 법제처 OPEN API 는 IP 등록이 필요하고 화면은 프레임이라
**브라우저(CDP)로 읽었다**. 다시 볼 일이 있으면 그렇게 한다.

## 화면 순서도

자치단체 한 곳을 보여주는 순서다. **규모 → 흐름 → 출처 → 선택 → 감시 → 이름.**

```
[들머리]  우리 동네 고르기 · 243곳                                    ✔
① 얼마나 큰 살림인가   세출·세입 결산 총액 · 세출예산 총액 · 1인당          ✔
② 10년 동안 어떻게     2009~2024 추이 · 지수 · 전년 증감            ◇ 뒤로 미룸
③ 어디서 오나          자립도·자주도 (당초/최종/결산 세 판) · 추경 증가율    ✔
④ 어디에 쓰나          비중 5종 — 금액과 분모까지                          ✔
⑤ 제대로 쓰나          수의계약·업무추진비·행사축제·의회경비 등             ✔
                       ★ 비율 옆에 반드시 금액을 — "1.2%가 몇 억인지"
⑥ 누가 받아 갔나       계약 업체 30곳 · 수의 몫                WCEGCF             ✔ (보조금은 ◇)
[진행]   지금 돌아가는 사업     세부사업별 예산현액·지출·재원         QWGJK        ✔
[원문]   예산서 · 결산서 · 재정공시                            BUDLK SETLK FINLK  ✔
[꼬리]   출처 · 갱신 주기 · 순위를 매긴 방식                                       ✔
```

`✔` 화면에 들어감 · `◇` 아직 없음

⚠️ **지금 보는 범위는 2026년 예산과 2024년 결산 둘뿐이다**(2026-09-21 결정).
시계열은 `data/series.json` 에 16년치를 받아 뒀지만 화면에서는 뺐다 — 나중에 볼 일이다.

## 기준을 섞지 말 것

**화면은 「결산 기준」과 「예산 기준」 두 판으로 나뉜다.** 섞으면 아무 말도 안 된다.

| 기준 | 무엇인가 | 최신 |
|---|---|---|
| **결산** | 이미 쓰고 확정된 실적. 다시 안 바뀐다 | 2024 |
| **실적** | 계약 실적(수의계약비율 하나) | 2024 |
| **예산[당초]** | 그 해 처음 짠 예산 | **2026** |
| **예산[최종]** | **추경까지 반영된** 예산. 추경이 끝나야 나와 한 해 늦다 | 2025 |
| 예산 | 당초/최종 구분이 없는 것들. API 가 시점을 안 밝힌다 | 2026 |

⚠️ **1차·2차 추경은 구분할 수 없다.** 지방재정365는 「최종예산」 하나만 준다.
당초와 최종의 차이가 곧 그 해 추경 전부다 — **전국 중앙값 +13.7%, 최대 +49.7%**.
당초만 보면 살림의 8분의 1을 놓친다.

⚠️ 당초는 2026 까지, 최종은 2025 까지다. **추경 증가율을 낼 때는 같은 해끼리 견줘야 한다.**

**⑤를 앞에 두지 말 것.** 수의계약비율부터 들이대면 맥락 없는 고발이 된다. 앞의 넷이
쌓아 준 무게를 업어야 한다. ⑥은 비율에서 이름으로 내려가는 마지막 칸이다.

⚠️ **비율만 보여주면 안 된다.** 비율 지표는 전부 세출결산액이 분모인데,
**금액 칸이 이미 같은 응답에 들어 있다**(`pfa_amt1`·`ane_stl_amt` 따위).
`rate` 한 칸만 뽑고 있어서 화면에 금액이 한 푼도 없었다 — 2026-09-21 에 발견했다.

## 갱신 주기

**수집은 매일(자동), 발표는 연 2회(사람).** 이 둘을 섞지 않는다.

- 발표 시점은 재정 달력에 붙인다 — **9월 결산 · 12월 예산 확정 · (선택) 6~7월 추경**
- 그래서 **연 단위 결산 계열이 1순위**다. 한 번 확정되면 안 바뀐다
- ⚠️ **일별 집행(`QWGJK`)을 2026-09-21 에 다시 올렸다.** 사용자가 「진행 중인 사업이 보였으면
  좋겠다」고 해서다. 그날 아침까지 "내렸다"고 적어 둔 것을 뒤집는다. 대신 **화면에 기준월을
  크게 박아 둔다** — 연 2회 갱신하는 사이에 숫자가 낡는 것이 이 축의 위험이다
- 다만 **계약(`WCEGCF`)·보조금(보탬e)은 매일 쌓는다.** 날짜로만 뽑혀서 안 쌓으면 소급이 안 된다.
  보여주는 건 결산 시즌에 "작년 1년치"로 묶어서
- ⚠️ **한 번에 다 받으려 하지 말 것.** 일일 트래픽 제한이 있을 수 있는데 실측이 안 됐다.
  기관에 묻는 대신 **조금씩 받아 채운다**(2026-09-21 결정). 그래서 수집기는 전부
  **이어받기**(이미 받은 것은 건너뛴다)와 **`--limit`**(이번엔 여기까지)를 갖춘다

## 데이터 지도 — 무엇을 말할 수 있고 무엇이 비었나

**→ `site/datamap.html`** (`python scripts/build_datamap.py`). **우리 작업용 화면**이다(2026-09-21 결정).

⛳ **2026-09-22 — 뼈대를 「질문」에서 「층위」로 갈아엎었다**(사용자 결정).
**질문 24개는 접었다.** 질문 문장은 짓는 사람 마음이라 부정확했다 — Claude 가 지어낸 것이었고,
축도 기울어 있었다(「공개를 제대로 하나」 하나에만 8개가 몰렸다).

**이제 뼈대는 자료의 알갱이 크기다.** 지어낼 여지가 없다 — 자료가 실제로 그 알갱이로 온다.

```
1층  큰 틀      총액 · 재정자립도 같은 지표        243곳을 한 줄씩 줄세우는 층
2층  사업별     세부사업 예산 · 집행               1층만 보여주는 곳들과 갈리는 자리
3층  낱줄       계약 한 건 · 보조금 한 건          ⛔ 2층과 안 이어진다
(옆) 공개 상태  올렸나 · 시민이 쓸 수 있나         알갱이가 아니라 「자료에 대한 자료」라 층 밖
```

- 지금 **23갈래 · 4층** — 찼다 7 · 반쪽 10 · 비었다 6.
- ⭐ **갈래마다 켜 셋을 매긴다** — **① 세상에 있나 ② 우리가 받아 뒀나 ③ 화면에 올렸나.**
  지금 **24 / 18 / 16** 이다. 「자료가 없다」와 「받아만 두고 안 올렸다」가 이제 갈린다.
  ③ 은 `site/` 밑에 **낼 자리를 미리 정해 두고** 거기 파일이 있나로만 본다(`낼자리` 표).
- ⚠️ **2층과 3층은 안 이어진다.** 사업과 계약을 잇는 열쇠가 없어 이름 맞추기는 2%만 걸렸다.
  한 층에 묶어 두면 화면이 「이어져 있다」고 거짓말하므로, **3층에 그렇게 박아 뒀다.**
- ⭐ **상태를 손으로 적지 않는다.** 저장소를 훑어 자료가 실제로 있는지로 판정한다 —
  보탬e 를 받아 오고 다시 구우면 ○ 가 저절로 ● 가 된다.
- **○ 마다 「필요한 자료 · 어디 있나 · 막힌 까닭」을 적는다.** 예: 나라장터 계약정보 —
  data.go.kr 조달청 API — `.env` 의 `DATA_GO_KR_KEY` 가 비어 있다.
- 사람이 적는 것은 셋뿐이다 — **층 나누기 · 자료가 어디 있나 · 반쪽인 까닭.** 스크립트 안에 글로 있다.
- ⭐ **자료를 전부 올린다**(2026-09-21 사용자 지적 — "이게 API 전부 아니잖아").
  **350줄** — 지방재정365 146 · 열린재정 198 · 그밖 6. 줄마다 우리 쪽 상태를 붙였다:
  **쓴다 32 · 따로 받아야 6 · 멈춘 자료 17 · 아직 안 씀 289 · 못 받는다 6.**
  우리가 쓰는 것만 보이면 **무엇을 안 쓰고 있는지가 안 보인다.**
  ⚠️ 쓰는 코드는 **지표 파일과 스크립트에서 꺼낸다** — 새 코드를 쓰기 시작하면 저절로 「쓴다」로 바뀐다.
- 갈래마다 **화면에 실제로 뜨는 칸 이름**(재정자립도[당초]·수의계약비율)과 한 줄 뜻풀이 62개를 붙였다.
- ⚠️ `catalog.html`(무슨 자료가 있나)과 헷갈리지 말 것. 그쪽은 **찾아보는 화면**(146종 검색)이고,
  이쪽은 **우리가 무슨 말을 할 수 있나 + 무엇이 안 들어왔나**의 지도다.

## 자료가 쌓여도 안 깨지게 — 해를 박아 두지 말 것

⚠️ **2026-09-21 사용자 못박음 — "나중에 데이터 추가되는 것까지 염두해야 하는 거 알제?"**

이 사업은 **자료가 계속 들어온다.** 계약은 날마다, 진행 중인 사업은 달마다, 지표와 공시는 해마다.
그런데 스크립트에 해를 박아 두면, 새 자료를 받아도 **화면은 옛 숫자를 그대로 내보낸다.**
오류가 안 나서 더 위험하다. 그래서 2026-09-21 에 전부 걷어냈다.

| 어디 | 옛날 | 지금 |
|---|---|---|
| `build_model_data.py` | `INDICATORS = '2024-2026'` | `data/indicators/` 에서 **제일 새 파일**을 고른다(`--지표` 로 고를 수도 있다) |
| `build_model_data.py` 원문 링크 | `fyr: '2025'` | 지표 파일이 말하는 예산연도에서 뽑는다 |
| `build_board.py` 계약 칸 | `계약해 = '2025'` | **12월 31일까지 받은 마지막 해**를 스스로 고른다 |
| ⑥ 판 첫 화면 | `계약해 = '2025'` | 같은 규칙으로 화면이 고른다 |
| `check_disclosure.py` | `FINLK fyr '2025'` | 올해부터 **거슬러 찾는다**(`--해` 로 못박을 수 있다) |
| `fetch_indicators.py` | 기본값 2024·2026 | **올해−2 / 올해** |
| `fetch_series.py` | 기본 끝 해 2024 | **올해−2** |

**규칙 — 해를 적어야 하면 자료에서 꺼내거나 오늘에서 뽑는다.** 사람이 해마다 고쳐야 하는 자리를
만들지 않는다. 화면에 박히는 「기준월·기준연도」는 **자료가 말하는 것을 그대로** 적는다.

### 다시 돌릴 때

```
python scripts/fetch_indicators.py              결산·예산 지표 (기본값이 올해를 따라간다)
python scripts/build_model_data.py --all        자치단체별로 묶기
python scripts/build_ongoing.py --all           진행 중인 사업 (그달 누계)
python scripts/build_contracts_summary.py --해 2026   계약 — 올해치만 다시 센다(1분)
python scripts/build_contracts_summary.py       계약 — 전수로 다시 센다(5분)
python scripts/check_disclosure.py              공시 링크 (원문 HTML)
node   scripts/recheck_disclosure.js            공시 링크 (크롬, 25분)
python scripts/build_transparency.py            공시 점검 화면 + 발자취 한 회차 쌓기
python scripts/build_board.py                   시황판
python scripts/build_model_page.py              자치단체 화면
```

⚠️ **계약을 `--해` 로 돌리면 다른 해는 건드리지 않는다.** 지난해 자료가 뒤늦게 고쳐졌다면
전수로 한 번 돌릴 것. 전수와 `--해` 결과가 같은지는 2026-09-21 에 맞춰 봤다(733칸 전부 같음).

### 공시 점검은 회차를 쌓는다

`build_transparency.py` 를 돌릴 때마다 `data/disclosure_history.json` 에 **한 회차씩** 쌓인다.
화면 「지난 점검과 견주기」 칸이 지난 회차와 견줘 **나아진 곳·나빠진 곳**을 이름으로 적는다.

- 공시는 **결산·예산에 맞춰 연 2회** 나온다. 이 지표의 값어치는 **바뀌었나**에 있다.
- ⚠️ **잣대가 다른 회차끼리는 견주면 안 된다** — 원문 HTML 만 본 회차와 크롬까지 본 회차는
  늘어난 것이 실제로 나아진 것이 아니다. 회차마다 `크롬날` 을 적어 두고 **화면이 스스로 경고**한다.

## 누가 무엇을 하나

⛳ **2026-09-22 에 다시 갈랐다. 저장소는 GPT 도 쓴다. 느슨하게 간다**(사용자 결정).
**막아 두지 않는다** — 파일을 나눠 잠그면 상대 쪽 버그를 보고도 못 고쳐 왕복이 생긴다.
텍스트 파일은 겹쳐도 git 이 줄 단위로 합친다.

**누가 주로 보나** — 실무(수집·스크립트·나머지 화면)는 **사용자와 Claude**,
**데이터 지도는 GPT**(`scripts/build_datamap.py` · `site/datamap.template.html` ·
생성물 `site/datamap.html`). 「주로」지 「만」이 아니다.

### 겹치면 진짜로 터지는 것 셋 — 이것만 지킨다

1. **구워진 HTML**(`site/*.html`) — 배포하려면 저장소에 있어야 해서 생성물도 커밋된다.
   둘이 각자 구우면 파일이 통째로 달라져 충돌한다.
   → **충돌은 풀지 말고 다시 구워서 덮어쓴다.** 그게 정답이다.
2. **엑셀과 `.gz`**(`docs/*.xlsx` · `data/**/*.json.gz`) — **git 이 못 합친다.**
   한쪽이 통째로 이기고 상대 작업이 조용히 사라진다. **손대기 전에 한마디 할 것.**
3. **폴더** — 같은 PC 의 같은 폴더를 쓰지 않는다. **따로 클론한다.**
   git 문제가 아니라 파일 문제다 — 굽는 중에 상대가 그 파일을 열고 있으면 섞인다.

⚠️ **데이터 지도를 고칠 때의 경계는 「자료 파일의 모양」이다.** 손으로 적는 화면이 아니라
**저장소를 훑어 스스로 판정하는** 화면이다. 읽는 것은 이것들이다 —
**모양을 바꾸면 GPT 에게 먼저 알린다:**
`data/indicators/*.json` · `data/series.json` · `data/contracts_summary.json` ·
`data/disclosure.json` · `data/disclosure_dom.json` · `data/disclosure_history.json` ·
`data/lofin365_146.json` · `data/coverage.json` · `data/openfiscal_198.json` ·
`site/data/ongoing/*.json.gz` · `site/data/contracts/*.json`

### 데이터 지도에서 깨면 안 되는 것 넷

1. **상태를 손으로 적지 않는다.** 저장소를 훑어 판정한다 — 보탬e 를 받아 오고 다시 구우면
   ○ 가 저절로 ● 로 바뀌어야 한다. 손으로 적는 순간 이 화면은 죽는다.
2. **추려서 보여주지 않는다.** 자료는 전수 350줄이다.
   («이게 API 전부 아니잖아» 로 한 번 퇴짜맞았다 — 우리가 쓰는 7덩이만 보여 줬을 때다.)
3. **용어가 나와야 한다.** 질문마다 실제 칸 이름(재정자립도[당초]·수의계약비율)과 한 줄 뜻풀이.
   («뭐가 들었는지 모르겠다» 로 한 번 퇴짜맞았다.)
4. **우리 작업용 화면이다**(시민용 아님). 파일 이름·명령을 그대로 적는다.

### 이번에 GPT 가 할 일 (2026-09-22 사용자가 정함)

**목적은 둘이다.**
1. **사람이 이해할 수 있는 형태로 만든다.** — 지금은 읽히지 않는다.
2. **자료가 추가되면 그것을 확인할 수 있게 한다.**

→ **범위(전수 350줄)는 그대로 둔다.** 고칠 것은 **읽히는 형태**와 **바뀐 것이 보이는가**다.
줄여서 읽기 쉽게 만드는 것은 2번 퇴짜의 반복이니 하지 말 것.

✔ **뼈대는 2026-09-22 에 Claude 가 층위로 갈아엎어 두었다**(위 「데이터 지도」 칸 참고).
질문 24개는 접혔다. **층 나누기는 사용자가 정한 것이니 GPT 가 다시 바꾸지 않는다** —
바꾸고 싶으면 사용자에게 먼저 말할 것.

- 「추가되면 확인」은 이미 같은 일을 하는 화면이 있다 — `transparency.html` 의
  **「지난 점검과 견주기」**. 회차를 쌓아 두고 지난 회차와 견준다(`data/disclosure_history.json`).
  같은 방식을 데이터 지도에 붙이면 된다.
- ⚠️ **지금 `○` 가 두 종류인데 한 가지로 칠해진다** — **자료가 없는 것**과
  **키가 `.env` 에 안 들어와서 못 받은 것**이다. 아래 「키」 칸 참고. 이 둘은 달리 읽혀야 한다.

### 그 밖의 버릇

- 앉으면 `pull`, 일어나면 `push`
- ⚠️ **`git add -A` 를 쓰지 말 것** — 상대 것이 딸려 들어가 커밋된다(전에 겪었다). 파일 이름을 대고 `add`.
- 「푸시했다」고 말하기 전에 `git log --oneline origin/main..HEAD` 로 **실제로** 확인한다.
  상대가 푸시하면 내 커밋도 같이 올라가서, "커밋만 했고 푸시는 안 했다"가 틀릴 수 있다.

✔ **지방의회 자료(CLIK)가 풀렸다 — 키가 나왔고 붙였다**(2026-09-22 저녁).
   위에 적혀 있던 「멈춰 있다·건드리지 않는다」는 **폐기한다.** Claude 가 가져간다
   (GPT 몫은 데이터 지도 그대로). 아래 「2026-09-22 — CLIK 4종을 붙였다」 칸을 볼 것.

⚠️ **인증키 값은 저장소에 적지 않는다.** 공개 저장소다. 점검표에도 「있음/없음」만 적고,
값은 `.env` 와 깃허브 Secrets 로만 간다.

점검표의 **「확인자」 칸**에 누가 본 것인지 적는다 — 직접 호출해 본 것과 화면만 본 것은
신뢰도가 다르다.

## 키

`.env` 에 넣는다(`.env.example` 참고). **깃에 올리지 않는다.**
지방재정365는 키 하나로 146종이 전부 열리고, data.go.kr 은 API 마다 활용신청을 따로 해야 한다.

⭐ **키를 읽는 자리는 `scripts/keys.py` 하나다**(2026-09-22). **환경변수 먼저, 그다음 `.env`.**
새 수집기를 짤 때 `.env` 를 직접 열지 말고 `from keys import 키읽기` 를 쓸 것.

⚠️⚠️ **여기서 한 번 데였다 — 일곱 스크립트가 `.env` 만 읽고 있었다.**
깃허브 액션에는 `.env` 가 아예 없고 Secrets 가 **환경변수**로 들어온다. 그래서
**Secrets 에 키를 넣어 두고도 봇에서는 못 읽는 상태**였다(`fetch_bizno` · `fetch_cleaneye` ·
`fetch_datagokr_all` · `fetch_revenue` · `probe_edu` · `probe_g2b` · `scan_datagokr`).
오류가 「키가 비어 있다」로만 나서 **키를 안 넣은 줄 알기 쉽다.** 2026-09-22 에 다 고쳤다.

⭐ **`python scripts/check_keys.py`** — 키가 다 있나 본다(호출 0회).
**`--호출`** 을 붙이면 키마다 한 번씩 진짜로 불러 본다. 값은 절대 안 찍고 길이만 찍는다.
매일 도는 봇도 이걸 첫 단계로 돌려서 **어떤 키가 살아 있나를 로그에 남긴다.**

⚠️ **점검표의 「있음」은 「쓸 수 있음」이 아니다.** 「확보」를 셀 때는 점검표가 아니라
`.env` 와 실호출을 볼 것. 2026-09-22 에 사용자가 키 둘을 주어 셋이 되었다.

⚠️ **깃허브 Secret 이름은 영문 대문자만 된다** — 한글을 못 쓴다. 그래서 무엇인지가 안 보인다.
**어느 것이 무엇인지는 이 표가 유일한 기록이다:**

| Secret 이름 | 한글로 | 누가 주나 | 받는 곳 |
|---|---|---|---|
| `LOFIN_KEY` | **지방재정365 키** | 행정안전부 | lofin365.go.kr |
| `DATA_GO_KR_KEY` | **공공데이터포털 키** (나라장터 계약·클린아이) | 조달청·행안부 | data.go.kr — API 마다 활용신청 따로 |
| `EDU_ALIMI_KEY` | **지방교육재정알리미 키** | 교육부 | eduinfo.go.kr |
| `CLIK_KEY` | **지방의정포털 키** (회의록·의안·의원) | 국회도서관 | clik.nanet.go.kr |
| `OPENFISCAL_KEY` | **열린재정 키** | 기획재정부 | openfiscaldata.go.kr — 아직 안 받음 |

- `LOFIN_KEY` — 지방재정365. 들어와 있고 쓰고 있다.
- `DATA_GO_KR_KEY` — **들어왔다(2026-09-22).** 나라장터 4종과 국고보조금이 여기 물려 있다.
  계약정보는 **실호출까지 됐다**(아래 함정 참고). 나머지 경로는 아직 안 눌러 봤다.
- `EDU_ALIMI_KEY` — **들어왔다(2026-09-22).** 집행내역까지 **실호출 됐다**(`scripts/probe_edu.py`).
- `CLIK_KEY` — **들어왔다(2026-09-22 저녁).** 지방의정포털 4종이 이 키 하나로 다 열린다.
  **실호출 됐다**(`scripts/probe_clik.py`). ⚠️ **하루 1,000회 · 한 번에 100건**이 박혀 있다.
- `OPENFISCAL_KEY` — **저장소 Secrets 에는 들어왔다(2026-09-22).**
  ⚠️ **`.env` 에는 빈 칸이다** — 손으로 돌릴 때는 못 쓴다. 쓰는 코드도 아직 없다(후순위).

키는 **두 곳**에 넣는다 — `.env`(손으로 돌릴 때)와 저장소 Secrets(매일 도는 봇).
**2026-09-22 기준 Secrets 에는 다섯 개가 다 들어갔다.** `.env` 에는 열린재정만 빠져 있다.

## 정해 주셔야 할 것

1. ✔ **단체의 「지자체 투명성 지표」는 없다** (2026-09-22 사용자 확인).
   "만들 줄 알았는데, 없음." **밖에서 올 것을 기다리지 말 것 — 우리가 만드는 것이 맞다.**
   지금의 세 켜(있나 · 닿을 수 있나 · 쓸 수 있나)가 그 초안이다.
   ⚠️ 다만 **투명성 지표는 여러 축 가운데 하나다.** 이걸로 사이트 전체를 짜지 말 것.
2. ⏳ **시황판 모양** — 고민 중. 막힌 까닭이 **「어떤 데이터가 있는지 모르겠다」**는 것이라,
   내가 먼저 **무엇이 있는지 읽을 수 있게** 정리해서 가져간다(`build_coverage.py`).
3. ⛔ **의견·제보 받을 곳은 먼저 꺼내지 말 것** (2026-09-22 사용자 지시).
   사용자가 먼저 말할 때까지 묻지 않는다. 단추 자리는 지금 그대로 둔다.
4. **순위를 광역/기초로만 가를지 · 집행률 붉은 칠 기준** — **무엇을 넣을지 정해진 뒤에** 다시 본다.
   (사용자 말 — "어떤 데이터들을 집어넣을지 확정하지 않아서 정하기 애매하다")
5. ✔ **보조금 받은 쪽이 개인이어도 일단 띄운다** (2026-09-22 사용자 결정).
   보탬e 를 붙일 때 가린 채로 두지 말고 그대로 보여주고, **화면을 보고 나서 다시 정한다.**

## 인계 — 실무자가 바뀌어도 돌아가게

사용자가 이 일을 계속 못 할 수 있다. **넘겨받는 사람이 이것만 있으면 굴릴 수 있게 한다**(2026-09-21).

**사람 손이 필요한 것은 셋뿐이다.**
1. `action@action.or.kr` 과 02-921-4709 로 오는 문의에 답하기
2. 해마다 두 번(9월 결산 · 12월 예산) **화면 다시 굽기** — 아래 네 줄이면 된다
3. 무엇을 이상하다고 할지 정하고 글로 쓰기 — 이건 넘길 수 없는 몫이다

```
python scripts/fetch_indicators.py <결산연도> <예산연도>
python scripts/build_model_data.py --all
python scripts/build_ongoing.py --all
python scripts/build_model_page.py
```

**나머지는 저절로 돈다.** `.github/workflows/daily.yml` 이 날마다 계약·세출을 받아 저장소에
쌓고, `pages.yml` 이 `site/` 를 깃허브 페이지로 올린다.

**넘겨줄 것** — ① 저장소 쓰기 권한(`github.com/bada17/local-finance`)
② 지방재정365 인증키(저장소 Secrets 의 `LOFIN_KEY` · 새 사람은 `.env` 에도 넣어야 한다)
③ 이 README. **판단의 까닭은 전부 여기 적혀 있다** — 그래서 문서를 딴 데 만들지 않는다.

## 기록

### 2026-09-22 — CLIK 4종을 붙였다 · 목록 전수는 22일짜리다

사용자가 인증키를 주었다(`.env` 의 `CLIK_KEY`). **네 갈래가 다 열렸다** — 회의록·의안·의원·지방정책정보.
「API 가 아직 안 나왔다」·「건드리지 않는다」고 적어 둔 것은 **폐기한다.**

**명세는 로그인 없이 볼 수 있었다.** `clik.nanet.go.kr/potal/guide/resourceCenter.do` 에
요청변수·출력값·샘플·공개 샘플키가 통째로 박혀 있다. **의회 243곳 코드표도 이 페이지 안에 있다**
(`rasmblyIdDownDiv`) — API 를 한 번도 안 부르고 받는다.

| 갈래 | 끝점 | 전수 | 100건씩 받을 때 |
|---|---|---:|---:|
| 회의록 | `openapi/minutes.do` | 887,070 | 8,871회 |
| 의안 | `openapi/bill.do` | 756,667 | 7,567회 |
| 의원 | `openapi/assemblyinfo.do` | 28,538 | 286회 |
| 지방정책정보 | `openapi/policyinfoList.do` | 493,736 | 4,938회 |
| **합** | | **2,166,011** | **21,661회** |

⚠️ **하루 1,000회 · 한 번에 100건.** 이 둘이 이 갈래의 전부를 규정한다.
하루 한도를 다 써도 **목록 전수에 22일**이다. 그래서 수집기는 **이어받기**와 **`--limit`**,
그리고 **날짜별 호출 수 기록**(같은 날 다시 돌려도 한도를 안 넘게)을 갖췄다.

⛔ **상세(`displayType=detail`)는 전수로 못 받는다.** 한 건에 한 번이라 2,166,011회 = **2,166일**이다.
   상세는 **사용자가 고른 범위만** 따로 받는다. 이게 이 갈래에서 제일 중요한 제약이다 —
   회의록의 값어치는 목록이 아니라 **상세에 들어 있는 발언 전문**에 있는데, 그걸 다 못 받는다.

**붙인 것**
- `scripts/fetch_clik.py` — 목록 4종 수집기. 이어받기 · `--limit` · `--갈래` · `--다시` · `--의회목록`
- `scripts/probe_clik.py` — 키 확인과 규모 세기(`--상세` 면 상세 칸을 전수로 찍는다)
- `scripts/spec_clik.py` — 명세를 표로 굽는다 → **`docs/CLIK_API_4종_명세.xlsx`**
  (갈래 4 · 요청변수 51 · 출력값 58 · 의회 248 · 의안상태 19 · 지역 18 · 오류 12)
- `data/clik/` — 받는 자리. 조각은 `<갈래>_00000.json.gz`(1만 건씩), 상태는 `상태.json`,
  코드표는 `의회목록.json`. **데이터 지도가 이미 이 자리를 보고 있어 ○ 가 저절로 ● 가 된다**
  (`받을자리` 의 `data/clik/*.json.gz`). GPT 쪽 파일은 건드리지 않았다.

**함정 — 명세와 실제가 어긋난다**(전부 `docs/CLIK_API_4종_명세.xlsx` 「출력값」 판에 적었다)
- **의원 목록은 명세에 `PPRTY` 인데 실제로는 `PPRTY_NM` 이 온다.** 명세대로 짜면 빈 칸이다.
- **의원 상세는 명세에 10칸인데 실제로는 15칸이 온다** — 생년월일·학력·경력·선거구·전자우편·팩스·
  페이스북이 명세에 없이 따라온다. ⚠️ 그중 **`OFFM_TLPHON`(사무실 전화) 칸에 사진 경로가 들어 있다.**
  칸 이름을 믿으면 안 된다.
- **의안 목록에 `RASMBLY_NM`(지방의회명)이 안 온다** — 명세에는 있다. `의회목록.json` 으로 붙여야 한다.
  거꾸로 `BI_NO`·`PROPSR`·`RASMBLY_NUMPR` 은 **명세에 없는데 온다.**
- ⚠️ **날짜가 없으면 빈 값이 아니라 `19700101`(또는 `19700101000000`) 이 온다.** 그대로 세면
  1970년에 처리된 의안이 수천 건 생긴다.
- ⚠️ **의안 제안일에 9자리가 섞여 있다** — `202211119` 를 봤다. 원자료 오류다.
- ⚠️ **날짜 거르개는 의안에만 있다**(`itncStartDt`·`itncEndDt`). 회의록·의원·정책정보는 통째로 훑는 수밖에 없다.
- ⚠️ **회의록 `searchType=PRMPST_CMIT_NM`(위원회명)은 명세에 있는데 ERROR06 으로 막힌다.**
  그래서 **API 로는 예결위만 골라 받을 수 없다** — 목록을 받아 `MTGNM` 으로 직접 걸러야 한다.
  나머지 셋은 멀쩡히 먹는다(「예산」으로 재 봤다): **안건명 `MTR_SJ` 254,188건** ·
  본문 `MINTS_HTML` 679,635건 · 의회명 `RASMBLY_NM` 3,278건.
  ⭐ **안건명 검색이 이 갈래에서 제일 쓸모 있는 거르개다** — 전수 887,070건을 254,188건으로 줄인다.
- ⚠️ **정책정보만 `displayType` 을 안 받고, 상세 끝점 이름도 다르다**(`policyinfoDetail.do`).
  **정렬 인자가 아예 없어**(등록일 내림차순 고정) 수집 도중 새 자료가 앞에 끼어들면 쪽이 밀린다.
  DOCID 로 거르지만 **빠뜨린 것이 생길 수 있어 다 받은 뒤 한 번 더 훑어야 한다.**
  나머지 셋은 **과거순 정렬로 고정**해서 이 표류를 피했다.

**의원 28,538건은 그날 전수로 다 받았다** — 네 갈래 가운데 유일하게 끝난 것이다(281회). 받아 보고 안 것 —

- ⭐ **현직 명단이 아니라 「역대 누적」이다.** 한나라당·새누리당·자유한국당이 그대로 나온다.
  서울 강남구의회가 **178명**으로 잡히는데, 구의회 정원은 스무 명 남짓이다. 지금 누가 의원인지
  알려면 **대수로 걸러야 한다** — 상세의 `MTGNM_KND_NM` 에 「…선거구(10대), …선거구(9대)」로 붙어 온다.
  ⚠️ 그런데 **그 칸은 목록에 없고 상세에만 있다.** 28,538번을 더 불러야 한다는 뜻이다(29일).
- ⭐ **`PPRTY_NM` 은 한 칸이 아니라 대수마다 반복되는 목록이다.** 「더불어민주당, 더불어민주당,
  더불어민주당」은 3선이라는 뜻이다. 28,538명에 칸이 **43,448개** — 쉼표를 세면 **선수(選數)가 나온다.**
  3선 이상 **3,461명**, 최다 **10선 1명**. 명세에는 한마디도 없는 칸이다.
- ⭐ **`정당코드없음` 19,430칸(44.7%)은 자료가 빠진 것이 아니다 — 정당이 진짜로 없던 때다.**
  광역과 기초로 갈라 보면 바로 보인다: **광역 3.3% · 기초 56.6%**. 기초의원 정당공천은 2006년
  5·31 지방선거부터고, 그 전에는 전원 무공천이었다. 그러니 **「빈 칸」으로 치면 안 된다.**
  ⚠️ 그렇다고 **무소속으로 바꿔 적어도 안 된다** — 공천 자체가 없던 것과 공천을 안 받은 것은 다르다.
  명시적 `무소속` 은 따로 **2,901칸(6.7%)** 있고, 광역 8.6% · 기초 6.1% 로 시기를 안 탄다.
- 펴서 센 정당이 **54가지**다. 자유당·민주자유당·신한국당·국민회의까지 나온다 — 아주 옛날 자료가 들어 있다.
- ⚠️ **`PHOTO_FILE_URL` 은 28,538건 전부 빈 값이다.** 명세에 있는 칸인데 쓸 수 없다.
  (사진은 엉뚱하게 상세의 `OFFM_TLPHON` 칸에 경로로 들어 있다.)
- ⭐ **자료에 있는 의회 코드가 248개인데 코드표는 243곳이다.** 코드표는 「지금」 기준이라
  없어진 의회가 빠져 있고, **코드표만으로 이으면 다섯 곳이 조용히 사라진다** —
  전라남도의회(061001)·광주광역시의회(062001)는 전남광주통합특별시의회(065001)로 통합됐고,
  인천 동구(032006)·중구(032011)는 서로 통합, 충남 서산군의회(041900)는 폐지됐다.
  → `의회목록.json` 에 **`지금있나: false` 로 다섯 곳을 붙여 뒀다.** 이을 때 이걸 쓸 것.

**회의록·의안을 각 16,700건까지 받아 보고 안 것** (⚠️ 과거순으로 받는 중이라 **가장 오래된 쪽 표본**이다)

- ⚠️ **날짜가 없을 때 쓰는 값이 `19700101` 하나가 아니다.** 목록에서 `0` · `18000101` ·
  **`19000101`** 이 나왔고, 오타로 보이는 `11981124`(9자리)도 있다. **받은 33,400건이 전부 망가진 날짜였다.**
  → 날짜를 셀 때 **네 가지를 다 걸러야 한다.** 하나만 걸러도 1800년·1900년 기록이 화면에 뜬다.
- ⭐ **그래도 과거순 정렬은 그대로 간다.** 망가진 날짜가 맨 앞에 몰리지만 덩어리가 작다 —
  재 보니 **회의록 2~3만 건 · 의안 3~6만 건**(각각 하루 안에 지나간다). 최신순으로 바꿔서
  생기는 표류가 더 비싸다.
- ⭐ **자료가 생각보다 훨씬 멀리 간다 — 회의록은 1951년부터다.** 앞서 「1992년부터」라고 적었던 건
  표본을 잘못 본 것이라 고친다. 재 본 범위는 이렇다:
  **회의록 1951-09-14 ~ 2026-09-18(75년)** · **의안 1994-09-28 ~ 2026-09-21(32년)** ·
  **정책정보 2008년 ~ 2026-07(18년)** · 의원은 역대 누적(자유당·민주자유당까지 나온다).
  ⚠️ 맨 끝(가장 오래된 쪽)은 세 갈래 다 **망가진 날짜 덩어리**라 진짜 시작점은 더 앞일 수 있다.
- **회의록 회의명(`MTGNM`)에 예산결산특별위원회가 5.2%** 다. 전수로 치면 **4만 6천 건쯤**이고,
  상세를 다 받으면 **46일**이다. 좁혀도 싸지 않다. ⚠️ `기타` 24.9% · `-` 7.3% 로 **비어 있는 것이 3할**이고,
  `本會議` 같은 **한자 표기도 섞여 있다**(547건). 회의명으로 거르려면 표기를 먼저 맞춰야 한다.
- ⚠️ **의안상태(`CL_STD_NM`) 표기가 난립한다.** 같은 「가결」이 `원안가결` · `처리-가결(원안)` ·
  `가결` · `원안` · `원안의결` 다섯 가지로 온다. **`분류불가` 도 12.8%** 다.
  → 처리결과를 쓰려면 **먼저 정규화해야 한다.** 코드표(`BIR***`)는 따로 있는데 이 칸은 코드가 아니라 글자다.
- ⭐ **제안자(`PROPSR`)로 「누가 냈나」가 갈린다** — 시장 4,954 · 청도군수 1,308 · 청양군수 723 …
  집행부가 낸 것이 압도적이고, 의원 발의는 `의원` · `박철환 의원 외 8인` 꼴로 온다.
  **의원발의 대 단체장발의 비율**은 이 자료로 바로 낼 수 있다 — 감시 지표가 될 만하다.
- 의안종류는 **조례안 50.0% · 기타 25.7% · 동의/승인안 9.7% · 예산안/결산안 4.2%** 다.

**정책정보 16,700건을 받아 보고 안 것** (⚠️ 이쪽은 최신순 고정이라 **최근 쪽** 표본이다. 2024-03 ~ 2026-07)

- ⚠️ **처음에 「중앙부처·연구원이 섞여 지방과 멀다」고 적었던 것은 표본 3줄만 보고 한 말이라 틀렸다.**
  실제로는 **자치단체가 대부분**이다 — 제주도청 30.9% · 인천시청 14.7% · 전남도청 3.6% · 광주시청 2.5%.
  **지방의회 것도 있다**(부산시의회 2.9% · 충남도의회 2.8% · 전북도의회 2.4% · 울산시의회 2.4%).
  자료유형에 **`의정활동/감사자료` 13.8%** 가 따로 있다.
- ⭐ **진짜 약점은 다른 데 있다 — 수집된 곳이 47곳뿐이고 쏠림이 심하다.**
  제주와 인천 둘이서 **45.6%** 다. 243곳을 줄세우는 데는 못 쓴다. 「이 자치단체가 뭘 올렸나」를
  낱개로 볼 때만 쓸모가 있다. 게시일은 **16,700건 전부 성하다**(다른 갈래와 달리 날짜가 안 망가져 있다).

### ⭐ 2026-09-22 저녁 — 기간을 잘랐다. 22일이 4일이 됐다

⛳ **사용자 결정 — "과거에 내용은 필요없어. 올해가 9회 지방선거잖아. 8회까지의 자료만 일단 필요해."**
⛳ **그날 저녁에 한 번 더 — "9회분도 자동으로 들어오게 만들어줘. 9회는 계속해서 업데이트 될거니까."**

→ **기간은 2022-07-01 부터 「오늘」까지다.** 시작은 제8회 임기 시작이고, **끝은 못 박지 않는다.**
전수를 훑던 것을 이 기간으로 자르니 이렇게 준다:

| 갈래 | 전수 | 8회 임기만 | 호출 |
|---|---:|---:|---:|
| 의안 | 756,667 | **177,787**(API 가 정확히 세 준다) | 1,778회 |
| 회의록 | 887,070 | 약 129,000 | 약 1,290회 |
| 정책정보 | 493,736 | 약 36,800 | 약 368회 |
| 의원 | 28,538 | (날짜 칸이 없어 못 자른다 — 전수 그대로, 이미 끝남) | 0 |
| **합** | 2,166,011 | **약 343,600** | **약 3,436회 = 4일** |

⚠️ **대수(`RASMBLY_NUMPR`)로 거르면 안 된다.** 대수를 **의회마다 따로 센다** —
서울시의회는 12대인데 1991년에 생긴 기초의회는 9대다. 회차와 대수도 어긋난다
(제9회 선거 당선자가 제10대 의회다). **날짜로만 자른다.**

**갈래마다 자르는 법이 다르다** — `itncStartDt`·`itncEndDt` 거르개는 **의안에만** 있다.
회의록·정책정보는 **최신순으로 내려가며** 기간 밖을 버리고 기간 시작에서 멈춘다.

⭐⭐ **그래서 최신순 갈래는 매번 「앞머리 5쪽」을 다시 훑는다**(`앞머리쪽수`).
**끝날만 오늘로 바꿔서는 새 자료가 안 들어온다** — 이어받기 위치가 이미 앞을 지나쳐 있어서
아무리 기다려도 맨 앞에 올라온 새 회의록을 못 본다. 여기서 한 번 헛짚을 뻔했다.
앞머리 훑기가 이것과 **표류**(수집 중에 새 글이 끼어들어 쪽이 밀리는 것)를 한꺼번에 푼다.
이미 받은 것은 DOCID 로 걸러지니 헛일이 아니다.
⚠️ 끝날을 바꿀 때는 **`상태.json` 의 `다음시작` 을 0 으로 되돌려야 한다** — 안 그러면
전에 「기간 밖」이라고 건너뛴 것이 영영 안 들어온다(2026-09-22 에 회의록 900건이 그랬다).

⚠️ **해를 코드에 박아 두지 않았다.** 끝날은 돌릴 때마다 「오늘」로 잡히고,
`--기간 20100101 20260630` 처럼 주면 옛 자료도 받는다(위 「자료가 쌓여도 안 깨지게」 규칙대로).

**그날 한 일** — 기간 밖이던 회의록·의안·정책정보(각 21,200건, 1950~60년대 것과 망가진 날짜)를
지우고 새 기간으로 다시 받기 시작했다. **지운 것은 커밋 `d2186ab` 에 남아 있다.**
⚠️ 그날은 **하루 한도 1,000회를 다 써서**(수집 947 + 손으로 확인한 것) 의안 1,200건까지만 받았다.
회의록은 제9회 임기 900건을 버리고 2026-06-30 에 닿기 직전에 걸렸다. **다음 날 이어서 받으면 된다.**

**아직 안 정한 것 — 무엇을 화면에 올릴지는 사용자가 정한다.** 지금은 「다 받아 두고 목록을 낸다」까지다.

### 2026-09-22 — 데이터 지도 읽기와 변경 확인 (GPT)

- 기존 데이터 지도 화면을 개편했다. 3층과 층 밖 공개 상태, 23갈래·전수 350줄·뜻풀이 62개를 유지한다.
  갈래를 접어도 항목 이름과 제공처·수집·화면 상태가 보이고, 제공처·활용 상태·내용으로 전체 자료를 찾는다.
- 열린재정 원본의 `odtNm`·`cls`·`epl`을 읽어, 빠져 있던 198개 자료의 이름·분류·설명을 복구했다.
- 별도 파일을 만들지 말라는 사용자 지시에 따라 **변경 기록은 기존 `site/datamap.html` 안에 보존한다.**
  `python scripts/build_datamap.py`를 실행하면 첫 상태를 저장하고 이후 추가·삭제·내용·상태 변화를 비교한다.
  같은 상태는 중복 기록하지 않는다. **생성물이 충돌하면 공유 저장소 최신 HTML을 복원한 뒤 다시 굽는다.**
- 새 수집 위치는 스크립트의 `받을자리`·`낼자리`에서 관리한다. 미등록 자료도 파일 변화로 잡힌다.
  목록·명세만 받은 열린재정은 ‘목록’으로 표시하고, 파일의 존재와 실제 활용 범위를 구분한다.

### 2026-09-21 — 정해진 것들 (사용자 결정)

물어본 17가지 중 열셋이 정해졌다.

**받는 것**
- **계약은 3년치를 소급해 받는다**(2024·2025·2026). "그래야 지금이 어떤지 보인다."
  `backfill_contracts.py` 가 하루씩 채운다 — **이어받기가 되니 끊겨도 다시 부르면 된다.**
- **보탬e 지방보조금은 전수로 받는다.**
- **국고보조금도 붙여 본다** — 다만 "쓸 만할지는 모르겠다"는 단서가 붙었다.
- **교육청(지방교육재정알리미)은 별도 꼭지로 하긴 한다.** 지금은 아니다.
- ✔ **지방의회(CLIK)는 Claude 가 가져간다**(2026-09-22 저녁에 풀렸다). 목록 4종을 전수로 받는 중.

**드러내는 것**
- **계약 업체는 이름까지만.** 대표자·사업자번호는 뺀다. "필요할 때가 있겠지만 일단 뺀 형태로."

**사이트**
- **서브도메인으로 간다. 다만 아직 가안이다 — ⛔ 도메인 연결은 사용자가 말할 때까지 하지 말 것.**
- **첫 화면은 사례로 연다** — 들여다본 사례 하나를 앞에 놓고, 그 아래로 내 동네 찾기.
- **바뀐 구역(광주+전남·인천)은 따로 두고 안내만 한다.** 2026 결산이 나오면 자연히 풀린다.
- **시계열은 5년치만. 그리고 결산용과 예산용을 따로 간다**(2026-09-21 추가 결정).
  결산 시계열 **2020~2024**, 예산 시계열 **2022~2026** — 한 그림에 섞지 않는다.
- **자동 수집은 켠다.** "호출이 정해져 있다면 켜놓고 데이터 모으는 게 필요하다."
  → **2026-09-21 에 켰다.** `.github/workflows/daily.yml` 이 한국 시간 새벽 5시 20분에 돌고,
  인증키는 저장소 Secrets 의 `LOFIN_KEY` 에 넣었다(사용자 승인). 못 채운 지난날도
  **하루에 30일씩만** 조금씩 메운다.
- **계약 원본은 저장소 안에 둔다**(3년치 165MB). 기관이 자료를 내려도 우리에게 남는 것이 값어치다.
- **보탬e 는 2025년치부터.** 받아 보고 쓸 만하면 거슬러 올라간다.
- ⏳ **11월 초에는 얼추 뭔가 정해져 있어야 한다**(변화의 시나리오 일정). 이게 사실상의 기한이다.
- **여수섬박람회는 실험이었다** — "사업을 쪼개서 볼 수 있는지 확인한 것뿐"이고
  **구체적 기획은 아직 없다.** 사례 꼭지를 지레 짜 놓지 말 것.

**주기·한도**
- ⚠️ **일일 트래픽 제한은 기관에 묻지 않는다.** 사용자 판단 —
  **"할 수 있을 만큼 조금씩 받아서 채우면 된다."** 그래서 수집기는 전부
  **이어받기(이미 받은 것은 건너뛰기) + `--limit`** 를 갖춘다. 한 번에 다 받으려 하지 말 것.
- **열린재정 `ERROR-310` 문의는 후순위.**

**사이트의 성격 — 사용자 말 그대로**
- **"이 사이트는 감시 쪽임."** 다만 **"예산에는 기본적인 데이터들이 많으니 그것을 설명하긴 해야 한다."**
  그리고 **"예산에 대한 감시 영역이나 교육 영역의 플랫폼으로 작동하게 만들 생각"**이다.
  → 감시가 목적이고 설명은 그리로 데려가는 길이다. 설명만 하는 안내서가 되면 안 되고,
  맥락 없이 순위부터 들이대도 안 된다.

**첫 독자 — 이게 제일 크다**
- 이 사이트는 **아름다운재단 「변화의 시나리오」 사업과 엮인다.**
  띄우고 끝이 아니라 **그것으로 교육을 하고 공론장을 연다.**
  → 혼자 보는 대시보드가 아니라 **여럿이 놓고 이야기할 판**으로 짠다.
  사례 하나를 끝까지 따라가는 꼭지가 그래서 쓸모가 크다.

**목적 — 2026-09-21 저녁에 사용자가 풀어 준 것** (맨 위 「이게 무엇인가」로 옮겨 적었다)
- 단체가 만들어 오던 **「지자체 투명성 지표」의 확정판**이다. ① 무엇이 공개되고 안 됐는지
  가리고 ② 공개된 자료에서 이상한 것을 본다.
- ⭐ **기초단체 전체 시황을 보여주는 판이 세상에 없다.** 그 판을 만드는 것이 먼저고,
  무엇을 이상하다 할지는 그때마다 갈린다.
- **주체는 함께하는 시민행동.** 다른 단체와 협업·연대는 하되 방향은 우리가 잡는다.
- **시민 참여를 원한다** — 보고 재미있어하고, 의견 남기고, 제보하는 것까지.
- **성공은 회원 확대와 이슈화.** ⛔ **교육은 아직 하지 않는다.**
- ⏳ **11월 초 = 계획서를 쓸 만큼 준비.**
- **문의처를 화면 꼬리에 넣었다** — 02-921-4709 · action@action.or.kr (응대는 사용자가 한다).

**아직 못 정한 것** — 보조금 수령자가 개인일 때 · 집행률 붉은 칠 기준
(위 「정해 주셔야 할 것」에 남겨 뒀다).

### 2026-09-21 — 무엇이 있고 무엇이 멈췄나 (146종을 전수로 세어 봄)
「어떤 데이터가 있는지도 모르겠다」는 말에 답하려고 **146종을 하나씩 눌러 자치단체별로 세었다**
(`build_coverage.py` → `data/coverage.json`, 다시 굽는 데 1분). 읽는 건 `report_coverage.py`.

- **138종을 셌다.** 못 센 8종은 날짜가 필요한 일별 축 넷과 2만 행 넘는 큰 것 셋, 링크 하나다.
- **자치단체 단위 130종 · 시도 단위 5종 · 지역 구분 없는 것 3종.**
  ⚠️ 시도 단위로 오는 것을 「안 냈다」로 읽으면 안 된다.
- **자료가 오는 해** — 2026년까지 44종, 2024년까지 72종, 2022년 이전에서 멈춘 것이 16종.

⚠️ **「146종」은 부풀려진 수다 — 같은 자료가 연도 구간으로 쪼개져 있다.**
`기관운영 업무추진비`(2019년까지)와 `기관운영업무추진비(2020년 이후)`(2026년까지)가 **다른 서비스**다.
지방보조금은 아예 셋으로 갈려 있다(~2019 / 2020 / 2021년 이후). **화면에 뿌릴 때는 계열로 이어붙여야 한다.**

⚠️ **그래서 「2019년에서 멈췄다」는 대부분 착시다.** 처음에 그렇게 적었다가 뒤집었다 —
업무추진비·지방의회경비·지방보조금·일숙직비·통이반장 보상금은 **모두 새 서비스로 이어져 2026년까지 온다.**

**정말로 이어받는 것이 안 보이는 자료는 여섯이다.**

| 멈춘 해 | 무엇이 | 왜 아쉬운가 |
|---|---|---|
| **2022** | 업무추진비 절감률 · 지방의회경비 절감률 · 행사축제경비 증감률 | **「작년보다 아꼈나」를 말해 주던 셋.** 비율(~2024)은 남았지만 증감은 끊겼다 |
| **2022** | 통합유동부채비율 | 지자체 유동성 위험을 재던 것 |
| **2018** | 재난관리기금 전출금 확보 여부 | 법정 의무를 지켰나를 ○/× 로 주던 몇 안 되는 자료 |
| **2017** | 맞춤형복지비비율 | 2024년치 「맞춤형복지비 시행경비」가 비슷하나 같은 지표는 아니다 |

⚠️ **「곳수가 적다 = 안 냈다」도 아니다.** 청사신축원가 39곳은 그해 청사를 지은 곳이 39곳이라는
뜻이고, 교육비특별회계 17곳은 교육청이 17개라는 뜻이다. **무엇을 내야 하는 곳인지부터 가려야**
투명성 지표가 된다. 지금 그 목록이 없다 — 단체가 쓰던 지표를 받으면 거기서 나올 것이다.

### 2026-09-18 — 데이터 확보
- 지방재정365 146종, 열린재정 198종의 **서비스코드를 전부 확보**했다. 둘 다 SPA 라 목록 API 를 직접 찾아 긁었다.
- 열린재정은 접두어가 `OPFI`/`TB_OPFI`/`OPFB` 셋이고 `VW_` 가 붙는 게 25개 섞여 있다. WAF 가 curl 을 막아 **브라우저 User-Agent 가 필요**하고, 상세는 `www` 가 아니라 **`opms` 호스트**로 가야 POST 가 통한다.
- 열린재정 107종이 `ERROR-310` 을 뱉는다. **주요·통합재정통계는 100% 되는데 상세재정통계·월간재정동향은 통째로 안 된다.** 키 권한 범위인지 미공개인지 모른다 — 문의하면 풀릴 사안.
- 열린재정은 전부 중앙정부 총량이라 **자치단체로 안 쪼개진다.** 우리 사이트엔 비교 맥락용이다.
- 나라장터 계약정보에 **업체명·대표자·사업자번호·수의계약 여부**가 나온다. 지방재정365에 없는 "누가 받았나"가 여기 있다.

### 2026-09-21 — 지방재정365 데이터 지도
**146종이 전부 열린다.** 하나씩 눌러 보고 공식 명세까지 받아 지도로 만들었다.

- 명세는 화면 조각으로만 온다. `…/dtstSvi/retvDtstDtsApi.do` 에 **폼 인코딩**으로 `pdtaId`
  하나만 POST 하면 요청인자 표와 출력값 표가 든 HTML 이 통째로 온다(JSON POST 는 안 먹는다).
  **146/146 받았다.** 출력 항목에 한글 이름이 붙어 있어, 이게 없으면 필드 뜻을 알 수 없다.
- 필수인자는 `fyr` 하나뿐인 것이 101종, 아예 없는 것이 40종이다. 나머지 다섯이 날짜·코드를 받는다.
- **일별 축은 넷** — `QWGJK` 세부사업별 세출 · `HCFDA` 분야별 세출 · `SIHHC` 세입현황 ·
  `WCEGCF` 계약현황. 필수인자 이름이 제각각이라(`exe_ymd`·`txrv_ymd`·`smz_ctrt_ymd`)
  명세를 보기 전에는 못 맞힌다. 이 넷 때문에 처음엔 "안 되는 서비스"로 잘못 적었었다.
- ⚠️ **`WCEGCF` 계약현황에 업체명·계약방법·계약금액·하도급유무가 243곳 전수로 있다.**
  「누가 받았나」는 나라장터에만 있는 줄 알았는데 지방재정365 안에 있었다. 점검표를 고쳤다.
- **인증키가 없으면 한 쪽에 5행 고정이다**(틀린 키는 `ERROR-290`). 243행짜리 연 단위 자료는
  키 없이도 받을 만하지만(49번), QWGJK 전국 46만 행은 9만 번이 되어 키가 있어야 한다.
  이 PC 에 `.env` 가 없다 — **키부터 넣어야 전수 수집이 시작된다.**

### 열린재정 — 후순위로 내림
2026-09-18 에 다음 일감으로 정했다가, 2026-09-21 에 뒤로 미뤘다. 중앙정부 총량이라
자치단체로 안 쪼개지는 것이 이유다. 그 전에 풀어 둔 것과 남은 것은 이렇다.

풀린 것: 명세 엔드포인트 `opms…/op/ko/sd/dtsStatsAcol/selectAcolViewList.do` 에
JSON `{"odtId":…, "rlsSvTyCd":"A", "odtSvSeq":"2", "odtNm":<possDtaNm>, "paramVO":{"odtNm":<possDtaNm>}}`
→ 요청인자·출력필드·일일제한. **161/198 받아서 엑셀에 넣었다.** 일일제한은 서비스당 1,000회.

- **`ERROR-310` 107종은 키 문제가 아니다**(2026-09-21 확인). 키를 아예 빼고 불러 보면
  되는 서비스는 `ERROR-300`(필수값 누락), 안 되는 서비스는 `ERROR-310` 이 온다.
  **서비스 이름 검사가 키 검사보다 먼저**이므로, 권한이 막힌 게 아니라 그 이름으로 등록된
  API 가 없는 것이다. 이름 규칙(`VW_`·`TB_` 접두어)이 목록과 다르거나, 문의할 사안이다.
- 명세 못 받은 37종 중 35종이 그 107종과 겹친다. **겹치지 않는 건 `OPFI165`·`TB_OPFI193` 둘뿐**이라,
  「명세 37종 재시도」는 실제로는 2종짜리 일감이다.

### 2026-09-21 — GPT 가 계정 벽 뒤를 보고 왔다
맡겨 둔 3곳이 다 돌아왔다. 나라장터 계약과정통합공개까지 넷을 점검표에 옮겼다.

- **지방교육재정알리미가 「완료」로 올라갔다.** 실호출 성공. 예산공시 295종 + 결산공시 325종,
  17개 시·도교육청. 2024년 집행내역 2,220건에 **보조사업명·보조사업자·집행액·최종정산액**이
  나온다 — 받는 쪽 이름이 실제로 나오는 몇 안 되는 소스다. 다만 학교운영비·재정결함지원·
  사립유치원지원은 교육청별 집계라 **개별 학교명이 없다.**
  ⚠️ **업무추진비 비율이 1% 미만에서 0 으로 잘린다** — 비율 말고 원금액으로 다시 계산할 것.
- **CLIK 은 공개 샘플키로 이미 열린다.** 회의록·의안·의원·지방정책정보 4종. 명세상 범위가
  회의록 광역 17 + **기초 175**, 의안·의원 광역 17 + 기초 125 다. 회의록 상세에 **발언 전문**과
  원본 파일 URL 이 붙어 있고, 예산·결산·보조금·수의계약·업무추진비·행정사무감사 **본문 검색이
  된다.** 2026-09-18 자료까지 들어오니 살아 있다. 사용자 키는 승인 대기.
- **국고보조금은 「기획예산처_국고보조금 정보」(15097584)** 였다. 보조사업자정보공시·중요재산
  정보공시·보조사업현황 세 갈래고, 보조사업현황에 **국고보조금·지자체부담금·자부담금**이 같이
  나온다 — 보탬e(지방보조금)와 대칭으로 볼 수 있다. 본체 호출은 아직 안 해 봤다.
- 나라장터 계약과정통합공개는 물품·용역·공사·외자 **4개 경로**까지만 확인. 실호출은 남았다.

⚠️ **셋 다 아직 「부분」이거나 검사가 남았다.** 명세를 읽은 것과 데이터를 받아 본 것은 다르다 —
점검표 「확인자」 칸에 그 차이를 적어 뒀다.

### 2026-09-21 — 진행 중인 사업 · 여수세계섬박람회로 시험해 봄
사용자가 「진행 중인 사업에 대한 뭔가가 있으면 좋겠다」고 해서 판을 하나 더 붙였다.
**결산 기준 / 예산 기준 / 진행 중인 사업** 셋이 되었다. 시험 사례는 2026여수세계섬박람회다.

- **`QWGJK` 하나로 사업 한 건이 끝까지 보인다.** 세부사업마다 예산현액·지출(누계)·집행률과
  **국비·시도비·시군구비·기타**가 같이 온다. "뭐가 보조금이냐"는 이 재원 칸이 답한다.
- 여수시 2026년 「박람회」로 걸리는 사업이 **42개 · 예산현액 306억 · 지출 257억(84%)**.
  재원은 국비 60억 · 도비 68억 · **시비 178억** — 시 돈이 절반을 넘는다.
  전남본청 2025년에도 「박람회」 사업이 28개 136억 있다. **도와 시가 따로 잡히니 합쳐 봐야 전체가 보인다.**
- ⚠️ **계약과 사업을 잇는 열쇠가 없다.** 계약현황(`WCEGCF`)에는 계약명만 있고 세부사업코드가 없다.
  9월 2일 여수시 계약 11건 중 2건이 섬박람회 건이었는데(포토존 997만원 수의1인견적 등),
  찾은 방법은 **계약명 글자 맞추기**뿐이었다. ⑥번 칸의 제일 큰 숙제다.
- ⚠️ 전남본청은 2026년 일별 자료가 아예 없다(2025년은 3,877건 멀쩡). 그때는 「단체마다 올리는
  시점이 다른가 보다」라고 적었는데 **틀렸다** — 구역이 바뀐 것이었다(아래 2026-09-21 칸).
- ⚠️ **끝난 뒤의 정산은 한참 뒤에 나온다.** 행사축제원가회계(`EVFTCT`)는 2024년까지다
  — 섬박람회 총비용·수익·순원가는 2028년쯤에야 여기 잡힌다. 그때까지는 위의 집행 숫자가 전부다.
- 🐞 **화면에 `<meta charset>` 이 없었다.** 깃허브 페이지가 헤더로 UTF-8 을 붙여 줘서 여태 몰랐는데,
  로컬 `python -m http.server` 로 열면 크롬이 CP949 로 읽어 **자바스크립트가 SyntaxError 로 죽는다.**
  `<!doctype>`·charset·viewport 를 template 에 넣었다.

### 2026-09-21 — 243곳 아무 데나
부산·옥천·여수 셋만 구워 두던 것을 **243곳 전부**로 늘렸다. 화면 위 고르개에서 고른다
(광역 17곳이 맨 위, 그 밑에 시도별로 기초 226곳). 주소에 `#4612000` 을 붙이면 그 곳으로 바로 열린다.

- **페이지에 다 담지 않는다.** 243곳 지표가 2.3MB 라 첫 화면이 무거워진다.
  박히는 것은 **목록 23KB** 뿐이고 고른 곳의 파일만 그때 받아 온다.
- 진행 중인 사업도 243곳으로 늘렸다. 이쪽은 전국을 **한 번에 받아 쪼갠다** —
  `laf_cd` 를 빼면 243곳이 함께 오므로 호출이 243분의 1이 된다(그 대신 한 번이 20분쯤 걸린다).
- 파일이 243개가 되니 **칸 이름을 빼고 줄줄이 늘어놓는 꼴로 바꿨다.**
  분야·부문·회계는 이름표의 몇 번째인지만 적는다. 그냥 담으면 한 곳에 500KB 가 넘는다.
- 진행 중인 사업에 **재원 거르개**(국비 든 것 · 시도비 든 것 · 제 돈만)와
  **집행률 낮은 순** 정렬을 붙였다. 「뭐가 보조금이냐」를 이 거르개가 가른다.
- 주소에 판과 찾을 글자까지 담는다 — `#4612000,진행중,박람회` 를 열면 섬박람회 사업 42개가 바로 뜬다.

### 2026-09-21 — ⚠️ 2026년에 구역이 바뀌었다 (지표와 진행 자료의 기준이 다르다)
243곳을 전수로 받아 맞춰 보다가 **네 곳이 어긋났다.** 이것을 모르면 숫자를 잘못 읽는다.

| 지표(2024 결산·2026 예산)에는 있는데 진행 자료가 없다 | 진행 자료에는 있는데 지표가 없다 |
|---|---|
| 광주본청 `2900000` · 전남본청 `4600000` | **전남광주본청 `6100000`** — 사업 6,357개 · 20.76조 |
| 인천중구 `2811000` · 인천동구 `2812000` | 인천영종구 `2811500` · 인천제물포구 `2812500` · 인천검단구 `2818500` |

- ✔ **2026년 7월쯤 실제로 통합됐다**(사용자 확인). **합친 상태로 계속 간다** — 되돌아가지 않는다.
- **광주와 전남이 하나로 올라온다.** 2026년 일별 자료에 광역 본청이 **17곳이 아니라 16곳**이고,
  「전남광주본청」의 예산현액(20.76조)이 옛 광주+전남과 맞는다. 광주 기초 5곳과 전남 기초 22곳은 그대로다.
- **인천은 구가 갈렸다.** 중구·동구가 없어지고 영종구·제물포구가, 서구에서 검단구가 나왔다.
- **그래서 같은 화면에서 기준이 섞인다.** 결산·예산 지표는 **옛 구역**, 진행 중인 사업은 **새 구역**이다.
  순위(226곳 기초 안에서 몇 위)도 옛 구역 기준이다. 섞어서 말하지 말 것.
- 화면에서는 이렇게 다뤘다 — 새 구역 넷은 고르개에 **「진행 중인 사업만」**으로 붙이고 결산·예산 단추를
  막았다. 옛 이름을 고르고 진행 판을 누르면 **새 이름으로 가는 단추**가 뜬다.

### 2026-09-21 — 계약과 사업은 이름으로 못 잇는다 (재 봤다)
⑥번 칸을 위해 **계약명과 세부사업명을 글자로 맞출 수 있는지** 여수시 9월 계약 109건으로 재 봤다.
**안 된다.**

| 어떻게 걸리나 | 건수 |
|---|---|
| ① 사업명이 계약명 안에 통째로 | **2건 (2%)** |
| ② 낱말 두 개 이상 겹침 | 25건 (23%) |
| ③ 낱말 하나만 겹침 | 72건 (66%) |
| ④ 하나도 안 겹침 | 10건 (9%) |

②도 믿을 게 못 된다. 「화정면 개도 테마관광자원 개발사업 청석포 진입로 정비공사」가
「화정면 여자도 도서식수원 개발사업」에 붙는다 — **면 이름과 「개발사업」이 겹쳤을 뿐 딴 사업이다.**
계약명은 부서가 쓰는 말이고 세부사업명은 예산서의 말이라 애초에 다른 언어다.

→ **자동으로 잇지 않는다.** 대신 나중에 **같은 글자로 계약도 함께 찾아 주는 칸**을 붙인다
(「박람회」를 넣으면 박람회 사업 42개와 박람회 계약 몇 건을 나란히). 잇는 척하지 않는 편이 정직하다.

### 2026-09-21 저녁 — 공시 점검 화면, 세 켜를 숫자로 재 봄

투명성 지표의 첫 판을 화면으로 냈다(`site/transparency.html`). **떨어지는 자리가 뚜렷하다.**

| 켜 | 곳수 | 떨어진 곳 |
|---|---|---|
| 재정공시 주소를 냈다 | **243** | — (지방재정365 `FINLK` 가 243곳 다 준다) |
| 눌렀더니 열렸다 | **217** | 26곳 |
| 붙은 파일이 잡혔다 | **131** | 86곳 — 목록을 자바스크립트로 그린다 |
| **엑셀·CSV 가 하나라도 있다** | **36** | **95곳이 PDF·한글 문서뿐** |
| (다른 축) 파일 이름이 뜻을 갖는다 | 41 | 90곳은 해시 이름 |

- ⭐ **③에서 무너진다.** 파일이 잡힌 131곳 가운데 **엑셀·CSV 를 낸 곳은 36곳뿐**이다.
  나머지는 숫자를 그림으로 냈다 — 시민은 다시 두드려 넣어야 한다. 이건 자동 점검의 한계가 아니라
  **눈으로 확인된 사실**이라, 화면에서도 이 칸만 `✕` 로 단정한다.
- **링크가 안 열린 26곳은 갈라 놨다** — 주소가 죽었다(404·400) 8곳 · SSL 로 자동 점검이 못 붙었다 15곳 ·
  너무 느리다 2곳 · 그밖 1곳. **뒤의 셋은 시민 브라우저에서는 열릴 수 있다.**
  (9/21 낮에 「404 여섯」이라고 적었는데 400·302 까지 넣어 세니 **여덟**이다.)
- **법 60조 16항목은 이름이 읽히는 41곳에서만 셌다.** 1번 세입·세출 운용상황이 73%,
  11번 주민참여예산 24%, 16번 지방보조금 10%, 나머지는 한 자릿수다. **한 파일에 묶어 낸 곳이 많아
  낮다고 안 낸 것이 아니다** — 화면에 그대로 적었다.
- **기계 쪽(③)은 오히려 멀쩡하다.** 「거의 모든 곳이 내는 자료」(기초 118종·광역 126종)를 잣대로 삼으면
  빠진 곳은 **9곳뿐**이고 그중 대구군위군이 14종이다(2023년 대구 편입 탓으로 보인다).
  → **자료가 없는 게 아니라, 시민이 보는 창구에서만 안 보인다.** 이게 이 화면의 한 줄 요약이다.
- 🐞 표 감싸개에 `overflow-x` 가 걸려 있으면 **그 칸이 sticky 의 기준**이 되어 243줄짜리 표의 머리줄이
  화면에 안 붙는다. 넓을 때는 풀고 좁을 때만 걸도록 고쳤다(다른 화면 셋은 아직 그대로다).

### 2026-09-21 밤 — 크롬으로 다시 보니 「못 봤다」가 절반으로 줄었다

`?` 를 줄이려고 243곳을 **크롬(CDP)으로 한 번 더** 열었다(`recheck_disclosure.js`, 25분).
파이썬이 못 본 것이 무더기로 나왔다.

| | 원문 HTML 만 | **크롬까지 합쳐서** |
|---|---|---|
| 링크가 열린다 | 217 | **234** |
| 붙은 파일이 잡힌다 | 131 | **183** |
| 파일 이름이 뜻을 갖는다 | 41 | **130** |
| 엑셀·CSV 가 있다 | 36 | **46** |
| 올린 날짜를 안다 | — | **81** |

- ⭐ **134곳이 크롬으로만 보인다.** 해시 이름 뒤에 글자 이름이 숨어 있었다 — 서울시는
  `67c141f1….pdf` 아홉 개가 「2025년 지방재정운영상황 재정공시」·「지역통합재정통계 보고서」로 바뀌었다.
- **17곳은 파이썬이 튕겼을 뿐 크롬에서는 열린다.** 인증서 사슬이 빠진 곳들이다.
  안 열리는 곳이 26곳 → **9곳**(주소가 죽음 7 · 인증서 1 · 느림 1)으로 줄었다.
- ⚠️ **그래도 ③은 그대로 무너진다** — 파일이 잡힌 183곳 가운데 **엑셀·CSV 를 낸 곳은 46곳뿐**,
  103곳은 PDF·한글 문서뿐이다. 크롬으로 더 봤는데도 비율이 안 올라간 칸이 여기다.
- 🐞 **`Page.loadEventFired` 를 기다리면 앞 페이지(about:blank)의 신호에 속아 빈 화면을 긁는다.**
  처음에 서울시가 「파일 0개」로 나온 까닭이 이것이었다. `readyState` 와 글자 수를 **눌러 보며** 기다리도록 고쳤다.
- 「첨부파일」·「바로보기」 같은 **단추 글자는 이름으로 치지 않는다**(`껍데기` 거르개).

### 2026-09-21 밤 — 계약 320만 건을 곳별로 묶었다 · 원자료에 자릿수 오류가 있다

`build_contracts_summary.py` 가 994개 파일(165MB)을 훑어 **자치단체 × 연도**로 줄인다(0.4MB).
시황판에 **「계약 3년치(우리가 센 것)」 축**으로 다섯 칸이 붙었다 —
계약 총액 · 수의계약 금액 비중 · 수의계약 건수 비중 · **상위 5개 업체 몫** · 거래 업체 수.

⚠️⚠️ **첫 집계가 2025년 978조원으로 나왔다. 원자료에 자릿수 오류가 있다.**

| 금액 | 어디 | 무엇 |
|---|---|---|
| **708.2조** | 전북 임실군 | 신덕 오궁리 상촌마을 진입로 개선공사 **관급자재** |
| **202.5조** | 경남 거창군 | 원동마을 안길 사면정비공사 관급자재(격자블록) |
| **13.6조** | 전남 고흥군 | 도화면 회전교차로 관급자재비 |

셋을 빼니 2025년이 **67.9조**가 되어 2024년(66.9조)과 맞는다. 진짜로 제일 큰 계약은
인천대로 도로개량 **8,211억**이다. → **1조원 넘는 줄은 빼고 세되, `수상한줄` 로 남긴다.**
지우면 안 된다 — **공시 품질에 대해 우리가 할 말이 이것 자체다.**

- **붙이기 전에 맞춰 봤다.** 우리가 센 2024년 수의계약 금액 비중 vs 지방재정365가 주는 `수의계약비율`(2024):
  **243곳 중 237곳이 10%p 안**, 차이 중앙값 +1.2%p. 20%p 넘게 벌어진 곳은 둘(인천미추홀구·부산금정구)이다.
  → 우리 셈을 믿어도 된다. 다만 **두 곳은 왜 벌어지는지 아직 모른다.**
- 전국 수의계약 금액 비중은 2024년 31.3% · 2025년 31.0% · 2026년(9월까지) 34.8%.
- ⚠️ 연도는 **계약일 기준**이고 **2026년은 아홉 달치뿐**이라 시황판에는 2025년만 올렸다.
- 금액이 0인 줄이 12,760개 있다. 아직 무엇인지 안 봤다.

### 2026-09-21 밤 — ⑥ 「누가 받아 갔나」 판이 섰다 · 시황판에 집행률

**화면 순서도의 마지막 빈칸 ⑥ 이 채워졌다.** 자치단체 화면에 판이 하나 늘었다
(결산 · 예산 · 진행 중인 사업 · **누가 받아 갔나**).

- **업체별로 묶어 30곳을 세운다** — 받아 간 금액 · 몫 · 건수 · 건당 평균 · **그 업체가 수의로 받은 몫.**
  해를 2024/2025/2026 으로 갈아 볼 수 있다. 자료는 `site/data/contracts/<코드>.json`(247곳, 2.1MB),
  화면은 **고른 곳 것만** 받아 간다.
- ⭐ **수의 몫 80% 위를 붉게 적는다.** 순천시 2024년을 보면 산림조합 63억·대창기업 37억이
  **100% 수의**다. 「수의계약이 많다」가 아니라 **「한 업체가 큰 금액을 수의로만 받아 갔다」**를
  집는 칸이다. 화면에도 그렇게 적었다 — 소액·단일공급은 법이 허용한다.
- **새 구역도 열린다.** 인천영종구처럼 2026년에 생긴 곳은 결산·예산 지표가 없지만
  계약 판은 선다(2026년치만).
- 시황판에 **진행 중인 사업 축**도 붙였다 — 예산현액 · **집행률** · 국비 비중(2026.09 누계).
  구역이 바뀐 넷(광주본청·전남본청·인천중구·인천동구)은 **빈칸이지 0이 아니다.**
- **금액 0인 계약 12,760건의 정체를 봤다** — 2025년만 4,928건(전체의 0.4%)이고
  일반경쟁 3,667 · 물품 4,277이 많다. 금액 칸을 그냥 비워 낸 계약이다.
  **금액 합계에는 영향이 없고 건수 비중만 0.4% 흔든다.**

### 2026-09-21 밤 — 「API 값과 20%p 벌어진 두 곳」을 쫓아가 보니 기준이 달랐다

앞 칸에 남겨 둔 숙제(인천미추홀구·부산금정구)를 열어 봤다. **우리 셈이 틀린 게 아니라
세는 기준이 다르다.** 두 곳 모두 **우리 쪽 전체 계약액이 API 분모의 두 배 가까이** 컸다.

까닭은 **다년계약이 두 줄로 들어오기 때문**이다. 미추홀구 생활폐기물 수집운반을 보면
같은 날(2024-12-31) 이렇게 들어와 있다 —

| 계약명 | 금액 |
|---|---|
| 2025년~2027년 생활폐기물…수집·운반대행(**1권역**) | **200억** |
| 2025년~2027년 생활폐기물…수집·운반대행(1권역)(**1년차**) | **66억** |

즉 **3년치 총액 줄과 그 첫 해 줄이 함께** 잡힌다. 미추홀구는 2024년 계약액의 **54%**가
이런 줄이고, 그래서 수의 비중이 46.6%(API)가 아니라 70.9%(우리)로 나온다.

- 전국으로는 **금액의 2.38%**(4.08조)뿐이라 합계에는 크게 안 튀지만, **한 곳에서는 판을 뒤집는다.**
- **합치거나 빼지 않는다.** 계약명에 기간이 적힌 것을 세어 `다년금액`·`다년몫` 으로 남기고,
  **10%가 넘는 곳은 화면이 스스로 경고**하게 했다(미추홀구 54.0%, 서울 구로구 45.7%, 성동구 36.0%).
- ⑥ 판 머리말에도 못박았다 — **「그 해에 나간 돈이 아니라 그 해에 맺은 계약의 총액」.**
- 꼬리말에 결산 판의 `수의계약비율` 과 왜 다른지 적었다. **둘 중 하나가 틀린 게 아니다.**

### 2026-09-21 밤 — 전수 점검: 무엇이 틀렸나

사용자가 **"완벽하게 하거나 문제가 있는 곳을 알려줘"** 라고 해서 처음부터 다시 훑었다.
**숫자를 실제로 재서** 찾은 것만 적는다. 고친 것과 못 고친 것을 갈라 둔다.

#### 고쳤다

| 무엇 | 실제로 어땠나 |
|---|---|
| 계약 원본 **자릿수 오류** 3줄 | 2025년 합계가 68조 → **978조**로 나갈 뻔했다 |
| **다년계약이 두 줄**(총액+1년차) | 미추홀구 수의 비중이 46.6% 대신 70.9%로 보인다 |
| **같은 계약원부번호가 여러 줄** | 29,470건 1,820억(1.06%). 울릉군 19.2% · 단양군 10.0% |
| 60조 매칭이 **연도를 안 봄** | 옹진군은 2015·2016·2017년 문서뿐인데 「기금 공시함」 ✔ 이었다 |
| **박힌 해 7곳** | 새 자료를 받아도 **오류 없이** 옛 숫자가 나가는 자리였다 |
| 공시 점검이 **덮어써짐** | 연 2회 재는 자료인데 변화를 못 봤다 → 회차로 쌓는다 |
| `board.html` **손 복사** | 템플릿만 고치면 조용히 어긋난다 → `build_board.py` 가 같이 옮긴다 |
| 🐞 `Page.loadEventFired` | 앞 페이지 신호에 속아 빈 화면을 긁었다(서울시가 「파일 0개」) |
| 🐞 `const 결과` 재대입 | 크롤 8분 다 돌고 마지막 줄에서 죽었다 |

**크롬으로 한 번 더 파고들었다**(주소의 `fileName=` 칸에서 이름 꺼내기 + 목록이면 글로 한 번 더 들어가기).
이름을 새로 건진 곳이 **22곳** 늘었다 — 이름읽힘 130 → **151**, 파일 안 잡히는 곳 51 → **40**.

#### 못 고쳤다 (또는 고치면 안 된다)

1. ⭐ **새 구역 넷은 공시 점검에 아예 없다.** 인천 영종구·제물포구·검단구, 전남광주본청.
   우리가 빠뜨린 게 아니라 **지방재정365가 주는 공시 주소 목록(`FINLK`)이 2026년치도 옛 구역 243곳뿐**이다
   (2026-09-21 에 2025·2026 둘 다 불러 확인). **새 구역 주민은 그 길로는 제 동네 공시에 못 간다.**
   → 화면 꼬리말에 사실로 적어 뒀다. 이건 고칠 게 아니라 **할 말**이다.
2. **아직 못 본 곳** — 파일이 안 잡히는 40곳, 이름을 못 읽는 43곳, 주소가 죽은 7곳.
   죽은 7곳은 손으로 찾아야 한다. ⚠️ 다만 **우리가 고쳐 주면 「지방재정365 링크가 죽었다」는 사실이 가려진다.**
3. **업체가 이름으로만 묶인다.** 사업자번호가 자료에 없다. 합치면 다른 업체를 한 곳으로 만든다 —
   규칙을 세우기 전에는 손대지 않는다.
4. **계약과 사업을 못 잇는다.** 열쇠가 없다(이름 맞추기는 2%만 걸려 접었다).
5. **금액 0인 계약 12,760건**(3년치). 금액 합계엔 영향 없고 건수 비중만 0.4% 흔든다.
6. **`2024-2026.json` 에 만든날이 없다.** 앞으로 받는 것에는 넣게 고쳤지만, 지금 것은 파일 날짜로만 안다.

#### 확인해서 「문제 아님」으로 지운 것

- 화면 여섯(자치단체별·진행·계약·시황판·자료 목록·공시 점검) 모두 **손전화 360px 에서 안 넘친다.**
  자바스크립트 오류도 없다.
- `--해` 로 한 해만 다시 센 계약 결과가 **전수 결과와 733칸 전부 같다.**

### 2026-09-22 — 나라장터 계약정보가 진짜로 붙었다 · 알리미는 서비스명에서 막혔다

사용자가 키 둘을 주었다. `.env` 와 저장소 Secrets 양쪽에 넣었다.

**나라장터 계약정보 — 됐다.** `probe_g2b.py` 가 빈 결과만 내던 건 키 탓이 아니라 **인자 이름**이었다.

- ⚠️ `inqryBgnDate`(8자리)가 아니라 **`inqryBgnDt`·`inqryEndDt`(12자리 `YYYYMMDDHHMM`)** 다.
  옛 이름으로 부르면 `08 필수값 입력 에러`, 12자리를 안 채우면 `06 DATE Format 에러`가 난다.
  **둘 다 HTTP 200 으로 온다** — 상태코드만 보면 성공처럼 보인다.
- ⭐ **사업자등록번호는 `corpList` 칸 안에 있다.** `^` 로 붙어 오고 **맨 끝이 사업자번호**,
  4번째가 업체명, 5번째가 대표자다. 공동수급이면 `]|[` 로 여러 업체가 이어진다.
- 2026-09-02 하루치 — 물품 2,922 · 용역 6,161 · 공사 5,329건.
  표본 50건씩 **모두 사업자번호가 붙었다.** 대표자 이름도 같이 온다.
- 다만 **업체 주소는 이 API 에 없다**(국가·지분율만 있다). 3층 「어떤 회사인가」는 번호·대표자까지다.
- ⚠️ 공사는 **계약명(`cntrctNm`)이 비어 오는 건이 있다.** 묶을 때 이름에 기대지 말 것.

**지방교육재정알리미 집행내역 — 됐다.** `scripts/probe_edu.py` 가 부른다.

- 호출 모양은 `?requestType=<서비스명>&key=<키>&type=json&pIndex=&pSize=` 다.
- ⭐ **서비스명은 `opclClsgExecList`.** 2024년 **2,220건**(GPT 가 말한 숫자와 같다),
  전체는 2014년부터 **35,886건**이다.
- ⚠️ **메타의 `dsId`(`VW_CLSG04090101`)는 서비스명이 아니다.** 그 이름으로 부르면 오류가 아니라
  **홈페이지 HTML** 이 온다 — 서비스명이 틀렸을 때 그렇게 된다. 이름부터 의심할 것.
- ⚠️ **`openapi.eduinfo.go.kr` 은 HTTPS 가 안 열린다**(연결 자체가 끊긴다). `http://` 로 불러야 한다.
  HTTPS 로 부르려면 `https://eduinfo.go.kr/portal/openApi.do` 를 쓰는데, 이쪽은 **JSON 을 한 번 더
  문자열로 싸서** 준다(두 번 풀어야 한다).
- 칸 8개 — 회계연도 · 교육청 · **보조사업명**(`AID_BIZ_NM`) · **보조사업자**(`AID_BIZR`) ·
  **집행액**(`AID_AMT`) · **최종정산액**(`FINAL_STTL_AMT`). 받는 쪽이 이름으로 나오는 드문 자료다.
  집행액과 정산액이 다른 줄이 흔하다 — **그 차이가 「돌려받은 돈」이다.**
- 고르는 인자는 둘, 둘 다 선택 — `FSCL_Y`(해) · `SD_EDU_OFFC_DIV`(교육청 코드, 서울 `B10`).
  한 번에 **1,000건**까지다(`336` 오류).
- ⚠️ **「총계」 줄이 섞여 있다**(교육청별 합계라 보조사업자가 비어 있다). 세기 전에 갈라낼 것.
- 서비스명을 찾은 길 — 데이터셋 화면의 Open API 탭이 부르는
  `POST /portal/service/openInfColViewPopUp.do` 에 `infId`·`srvCd=A` 를 주면 `apiRes` 로 온다.
  **다른 자료의 서비스명도 이 길로 꺼낸다**(공개 화면 목록은 500·404 로 막혀 있다).

### 2026-09-22 — 보탬e 와 클린아이를 받았다 · 주민참여예산은 정말 없다

「아직 안 받은 6묶음」 중 **셋이 줄었다**(나라장터·보탬e·클린아이). 남은 셋은 성격이 다르다.

**보탬e 지방보조금 — 받았다.** `scripts/fetch_grants.py`, 2025년치 `data/grants/2025.json.gz`.

- ⭐ **인증키가 없다.** 화면이 쓰는 창구를 그대로 부른다 — `POST /sp/aidBizInfoPbaInq`(JSON 본문),
  지역 코드는 `POST /sp/aidBizInfoPbaPry`. `pageSize` 는 1000까지 받는다.
- **243곳 전부 · 154,230건 · 교부액 9조 3,517억 원**(196초). 받는 쪽 이름 57,188가지.
- ⚠️ **파이썬 기본 설정으로는 TLS 악수에서 거절당한다**(`SSLV3_ALERT_HANDSHAKE_FAILURE`).
  curl 은 되는데 파이썬만 안 되니 주소·인자를 의심하게 된다. **암호모음 탓**이다 —
  `ssl` 얼개에 `DEFAULT@SECLEVEL=1` 을 주면 풀린다.
- ⚠️ **`lafWa`(광역)를 비우면 화면이 막는다.** 곳마다 따로 불러야 한다.
- ⚠️ **받는 쪽이 「비공개기관」인 줄이 992건**(0.6%, 241억 원). 지우지 말 것 —
  **「얼마나 가렸나」도 세어야 할 숫자다.**
- ⚠️ **곳을 이름으로 세지 말 것.** 「중구」가 6곳, 「동구」가 6곳이다 —
  이름으로 세면 243곳이 222곳으로 줄어든다. `_곳코드`로 셀 것.

**클린아이 지방공기업 — 받았다.** `scripts/fetch_cleaneye.py`, `data/cleaneye/2015~2025.json.gz`.

- 사용자가 2026-09-22 에 data.go.kr 에서 **셋을 활용신청**했다(자동승인, 2028-09-22 까지).
  키는 `DATA_GO_KR_KEY` 그대로다 — **API 마다 신청이 따로**라는 것을 다시 확인한 자리다.
- **연도마다 370~421개 기관.** 부채·자본·자산(경영정보), 당기순이익·영업이익(경영성과),
  부채비율·유동비율(주요경영분석지표).
- ⚠️ **끝점 번호와 오퍼레이션 번호가 엇갈린다** — `openApiMngInfo3/openXmlMngInfo2`,
  `openApiMngResult3/openXmlMngResult2`. 번호를 맞춰 부르면 `12 해당 오픈API 서비스가 없거나 폐기됨`.
- ⚠️ **`type` 이 필수다.** 빼면 `11 NOTEXISTPARAMETER_ERROR(type)` 이 나는데 키 문제로 보이기 쉽다.
  데이터포맷은 XML 이라 `type=json` 을 줘도 XML 이 온다.
- 아직 안 받은 칸 — **임직원 수 · 경영평가 등급**은 이 API 셋에 없다. 클린아이 화면에는 있다
  (`/user/mngEvaluGradeStat.do` 가 **기관별 431줄을 키 없이** 내준다).

**주민참여예산 — 전국 통합본이 정말 없다**(찾아보고 확인했다. 데이터 지도에 적힌 대로였다).

- [주민e참여](https://pb.lofin365.go.kr/)는 **각 자치단체 사이트로 보내는 관문**일 뿐이고,
  자료실 17건은 전부 우수사례집·홍보영상이다.
- data.go.kr 에도 연수구·광진구·서대문구처럼 **자치단체가 각자 올린 것뿐**이다.
- 다만 **교육청은 예외** — 알리미에 주민참여예산 화면이 있다
  (`/portal/theme/residPartcBudgState.do`). 17개 시·도교육청 것은 받을 수 있을 것으로 보인다.

### 2026-09-22 — 포털을 통째로 받아 놓고 보니 투자심사는 있었다

사용자가 **「다 검색한 거임?」** 하고 물었다. 아니었다. 그때까지 한 것은 **검색 화면을 긁어
낱말마다 첫 10건**을 본 것이었다. 그걸로는 **「없다」고 말할 수 없다.**

- 목록조회서비스(`15077093`)·검색서비스를 활용신청해서 **목록을 통째로 받았다** —
  오픈API **17,035건**(자료 11,991종·기관 407곳), 파일데이터 **211,981건**.
  `scripts/fetch_datagokr_all.py` · `data/datagokr_api_list.json.gz` · `..._file_list.json.gz`
- ⭐ **투자심사는 있었다.** `15051377 행정안전부_지방재정365_중앙투자심사결과` —
  행안부가 **전국 단위로** 낸다. 다만 **API 가 아니라 파일**이라 그동안 못 찾고 있었다.
  데이터 지도에 「API 로는 안 나온다」로만 적혀 있었던 것을 고쳤다.
  → **「없다」와 「우리가 못 찾았다」는 다르다.** 목록을 다 쥐기 전에는 가를 수 없다.
- **민간위탁은 자료로 없다.** 17,035건 중 걸린 4건은 전부 지방재정365 **설명문 안에** 그 말이
  들어간 것이지 민간위탁 자료가 아니다.
- ⭐ **자치단체는 자기 재정을 거의 안 연다.** 243곳이 직접 올린 재정 API 가 124종인데
  **행사 29 · 축제 23 · 예산 15 · 수의계약 3(전부 보령시) · 보조금 1(영주시).**
  **축제·행사 52종 대 수의계약 3종** — 이건 그냥 숫자가 아니라 **화면에 적을 사실**이다.
- 주민참여예산은 **파일 90건이 전부 자치단체가 각자 올린 것**이었다. 전국 통합본이 없다는 것을
  이제 눈대중이 아니라 전수로 말할 수 있다. 남은 길은 **제공신청·정보공개청구**다.
  ⚠️ 사용자 결정 — **자치단체 것을 하나씩 받는 일은 하지 않는다.** 전국적으로 통일된 것만 쓴다.
  **「왜 전국 분포가 없나」는 따로 논의할 거리**로 남겨 뒀다.
- 아직 안 받은 전국 단위 자료 가운데 눈에 띄는 것 —
  `15163532` 기획예산처_**보조사업별 수의계약현황**(보탬e 와 바로 붙는다),
  `15050832` 조달청_입찰공고 **기업별 투찰 및 계약내역**,
  `15126793` 기획예산처_국고보조금 집행 및 보조사업 현황,
  조달청 **누리장터 민간계약 3종**(나라장터 밖 계약이 여기 있을 수 있다),
  `15162856` 권익위_공공재정 부정청구등 신고처리현황.
- ⚠️ **이 표의 한계** — 낱말은 제목·설명·키워드까지 봤지만, 결국 **우리가 떠올린 말**로만 찾는다.

**지방교육재정알리미 서비스명 표도 만들었다** — `scripts/fetch_edu_catalog.py` ·
`data/eduinfo_catalog.json`. 자료 **638종 중 622종이 API 로 열린다**(143초).
고르는 인자는 대부분 `회계연도`+`지역구분`이라 지방재정365 146종과 부르는 꼴이 거의 같다.

### 2026-09-22 — 세입 판을 세웠다 · 교부세에서 깎인 3,374억

사용자가 **「세입 관련 자료 받아야 하는데 가능해?」** 하고 물었다. 키는 이미 있었다 —
지방재정365 146종 가운데 **43종이 세입 쪽인데 우리가 쓰던 것은 4종뿐**이었다.
화면이 **쓰는 쪽(세출·계약·보조금)에만 쏠려** 있었던 것이다.

- `scripts/fetch_revenue.py` → `data/revenue/` 43종 · **342,727줄**(359초).
  ⚠️ `SIHHC 세입현황` 은 `fyr` 만으로는 안 된다 — **기준일(`txrv_ymd`)이 필수**다.
- `scripts/build_revenue.py` → `site/data/revenue/<코드>.json` **257곳**.
  화면은 `index.html` 의 **「돈이 어디서 오나」 판**이다(`model.template.html`).

여섯 칸으로 짰다 — ① 얼마가 ② 어디서 ③ 무슨 세금인가 ④ 걷힌 것과 밀린 것
⑤ 깎아 준 돈 ⑥ 상과 벌. (판 이름은 **「세입」**이다 — 2026-09-22 사용자 지시.)

⚠️ **예산과 결산을 나란히 둔다**(사용자 지적으로 고쳤다). 처음에는 ② 재원 구성을
**결산(`FIACRV`)만** 쓰고 있었다. 예산 쪽 `FIRVBG`(2001~2026)를 나란히 붙였다 —
예산은 들어올 것으로 **잡은 것**, 결산은 실제로 **들어온 것**이라 섞으면 안 된다.
예산은 최종(추경 반영) 기준이라 결산보다 해가 앞선다.

⚠️ **API 로는 세목이 시·도 17곳까지만 나온다**(`KAAAE`). 기초 243곳 자료에서는 「지방세」가
**한 덩어리**다 — `FIACRV` 레벨3(가장 깊은 칸)까지 봐도 「지방세」로 끝난다.
⚠️ 시도 이름 꼴이 다르다 — 우리 자료는 「충북」인데 세목 자료는 「충청북도」다. 손으로 맞췄다.

⭐ **그 빈칸을 연감으로 메웠다**(2026-09-22, 아래 칸 참고). 이제 ③ 은 **244곳이 제 세목**이고,
못 이은 곳에서만 시·도 값을 보여 주며 「이 곳의 몫이 아니다」라고 적는다.

**「세목이 왜 광역까지뿐인가」를 따로 확인했다**(2026-09-22 사용자 물음).
⭐ **활용신청을 안 해서가 아니다.** 셋을 갈라 보면 이렇다.

1. **지방재정365에는 없다.** 146종은 `LOFIN_KEY` 하나로 전부 열리고 명세도 146종 다 봤다.
   세목 자료는 셋뿐인데(`KAAAE` 도시군세 세목별 비중 · `KAAAG` 세목별 징수율 ·
   `KAAAB` 세목별 세수신장률) 전부 **시·도 아니면 전국 합계**다.
   ⚠️ `KAAAE` 의 「시」·「군」 줄도 개별 시·군이 아니라 **그 시도 안의 시 전체/군 전체 합계**다
   (`wa_laf_cd` 만 있고 `laf_cd` 가 없다). 재원별 `FIACRV` 는 레벨3 까지 있는데
   **가장 깊은 칸이 「지방세」 한 덩어리**다.
2. **원자료는 있는데 문서다.** `15086648 행정안전부_지방세통계연감`(2026-03-25 갱신)이
   「제3편 과년도의 지방세 **세목별** 부과·징수 실적」을 싣는다. 그런데 **`hwpx`(한글 문서)**다 —
   기계가 읽는 표가 아니다. ⚠️ 이 연감이 **기초 자치단체까지** 내려가는지는 아직 못 봤다.
   딸린 낱말에 「지방세 시도별」이라고만 적혀 있다. **받아서 봐야 안다.**
3. **신청하면 열릴지 모르는 것 하나** — `15107409 행정안전부_통계연보_지역별·세목별 지방세 규모`.
   다만 **심의승인**이라 자동으로 안 열리고, 통계연보라 「지역별」이 시·도일 공산이 크다.

→ **공개 정책의 문제지 우리가 안 받은 것이 아니다.** 화면에도 그렇게 적었다.

⭐ **⑥이 머리기사다.** `LLBSR 지방교부세감액현황` 에는 **감액사유와 위반지출내역이 글로** 있다.

- 2012~2026 년 **243곳 전부**가 한 번 이상 깎였고 합계 **3,374억 원**이다.
- 사유 — **법령위반과다지출 1,242건 · 수입징수태만 450건 · 특별교부세 교부조건 위반 58건 ·
  예산편성기준위반 56건 · 재정투융자미심사 12건.**
- 내역이 구체적이다. 서울 2025년 27억은 **「투자심사 결과 부가된 조건을 이행하지 않고 사업추진」**
  이다 — 위의 투자심사 이야기와 그대로 이어진다.

② 의 「제 돈 몫」이 한 곳의 처지를 한 수로 말해 준다 —
**서울본청 59.5% · 부산본청 37.2% · 옥천군 9.7%**(전국 가운데값 15.7%).
③ 체납은 그 해 지방세 징수액에 견주었다(서울 4.4% · 옥천군 5.2%, 가운데값 5.5%).

⚠️ **보탬e 코드와 지방재정365 코드가 다르다.** 보탬e 는 서울특별시가 `6110000`·종로구가
`3000000` 인데, 지방재정365 는 서울본청이 `1100000` 이다. **보조금과 세입을 이을 때 반드시 걸린다.**

화면은 크롬을 CDP 로 띄워 값으로 확인했다(자바스크립트 오류 0 · 가로 넘침 없음).
⚠️ 이 컴퓨터의 헤드리스 크롬은 **561px 밑으로 안 내려간다** — 360px 검사는 못 했다.
표는 기존 판과 같이 모두 `.scroll` 칸 안에 있다.

### 2026-09-22 — 연감(한글 문서)에서 기초 자치단체 세목을 꺼냈다

API 가 시·도까지만 주는 자리를 **한글 문서로 메웠다.** `scripts/extract_taxitem.py` ·
`data/taxitem/<시도>.json` · 세입 판 ③ 에 붙였다(`build_revenue.py`).

- ⭐ **`.hwpx` 는 읽을 수 있다.** 옛 `.hwp` 와 달리 **zip + XML(OWPML)** 이라
  `Contents/section*.xml` 의 `<tbl>·<tr>·<tc>` 를 풀면 표가 칸 단위로 나온다.
  칸에 `cellAddr`(좌표)·`cellSpan`(병합)이 붙어 있어 **병합된 머리칸도 자리를 잡을 수 있다.**
- 28MB 파일에서 **자치단체 244곳 · 세목 이름 57가지**를 뽑았다(2024 결산, 단위 천원).
  ⚠️ 원자료를 대화창에 쏟지 않고 **세어서만** 봤다.
- **검산 — 16개 시도 중 15곳에서 「합계줄 = 본청 + 기초들」이 소수점까지 맞는다.**
  제주만 표 모양이 달라 합계 줄이 없다.
- 이 자료는 **결산물이다.** 「지방세 결산자료에 따른 통계연감」이고 **연 1회**,
  2024회계연도분이 **2026-03-25** 에 올라왔다 — **결산 뒤 1년 3개월쯤 걸린다.**

**걸린 함정 넷.** 다시 뽑을 사람이 같은 데서 헤매지 않도록 적어 둔다.

1. **이름을 「종 로 구」처럼 띄어 쓰고 칸마다 영문을 붙인다**(「소 계Sub-total」).
   낱말로 머리칸을 찾으면 다 어긋난다 → **첫 데이터 줄 바로 앞을 머리로** 잡는 규칙으로 바꿨다.
2. **시도 표가 넷으로 쪼개진다**(이름+시세 / 시세 뒷칸 / 구세 / 군세). 게다가 광역시 다섯 곳은
   **한 표 안에 시세·구세 블록이 위아래로 쌓여** 있어 같은 「중구」가 두 번 나오고
   **뒤엣것이 앞엣것을 덮어썼다** — 대구 중구가 3,039억 대신 667억(구세만)이 됐다.
3. **다른 편에도 시도별 시군구 표가 있다**(재산세 과세현황 `9-1-1.`, 자동차세 `10-1-1.`,
   비과세감면 `15-2-1.`). 그것까지 담으면 총괄과 상세가 섞인다 → **제2편 「1-x. <시도>」만** 쓴다.
4. 남의 표에 딸려 온 「세종시」를 걷어내는 규칙을 넣었다가 **경기도 광주시**를 광주광역시로 오인해
   -2.25% 가 어긋났다 → **세종만** 거른다. 같은 이름의 기초가 있는 한 이름만으로 시도를 못 정한다.

⚠️ **내 검산이 두 번 틀렸다**(자료가 아니라). 합계에서 **시도 본청 줄을 빼먹고** 더해 「경기 3곳」처럼
보였고, **`KAAAE`(시도세만)** 를 시도 전체와 견줬다. **검산식부터 의심할 것.**

⚠️ 이어 붙일 때 — 우리 이름은 「시도짧은+곳이름」(`서울종로구`)인데 연감은 「종로구」다.
본청은 연감이 「서울시」·「경기도」라고 쓴다. **인천 미추홀구는 연감이 옛 이름 「남구」를 쓴다.**

### 2026-09-22 — ⛔ 순계로 바꾸지 않는다 (재 보고 접었다)

세입 자료는 총계(`tott_sum_amt`)와 순계(`prsm_amt`)를 함께 준다. 순계가 더 정확해 보여서
바꿀까 했는데, **재 보니 거꾸로 읽히는 수였다.**

- 순계는 「제 돈으로 조달한 것」이 아니라 **「전국 합계를 두 번 세지 않으려고, 받는 쪽에서
  위에서 내려준 돈을 지운 것」**이다.
- ⭐ **인천 부평구 2024 — 보조금 7,685억 → 121억, 조정교부금 1,476억 → 0.**
  그래서 제 돈 몫이 **15.3% → 51.3%** 로 「뛴다」. 자립도가 높아진 게 아니라 **분모를 지운 것**이다.
- 전국 243곳 중앙값 **+9.4%p**, 최대 **+36.0%p** 가 전부 같은 착시다.
- → **총계를 쓴다.** 재정자립도·자주도 공식도 총계 기준이라 화면의 다른 지표와도 맞는다.

⚠️ 대신 **「보전수입등및내부거래」를 따로 떼어 적는다.** 이건 밖에서 새로 들어온 돈이 아니라
작년에 쓰고 남은 돈(순세계잉여금)과 회계끼리 주고받은 돈이고, **순계로도 거의 안 빠진다**
(부평구 1,322억 → 1,321억). 서울 19.2% · 옥천군 24.8% 나 되는 큰 덩어리다.
→ 표에 `※` 를 달고, **그것을 뺀 제 돈 몫**을 같이 낸다(서울 59.5% → 73.6%, 옥천군 9.7% → 13.0%).
화면 ② 아래에 **「순계로 바꾸지 않는다」는 까닭을 부평구 숫자와 함께 박아 두었다.**

### 다음에 여기서부터 (2026-09-21 저녁부터)

✔ **끝남** — 투명성 점검 화면 · 계약 3년치 채우기 · **크롬 재점검** · **계약 곳별 집계와 시황판 칸**.

✔ **끝남** — 시황판 집행률 칸 · ⑥ 판 · 금액 0 계약 · 박힌 해 걷어내기 · 공시 점검 회차 쌓기 ·
크롬 재점검 두 번째 판. **위 「전수 점검」 칸을 먼저 읽을 것.**

⭐ **1. 아직도 못 본 곳을 줄인다.** 크롬까지 돌리고도 남은 것 —
   파일이 안 잡히는 51곳, 이름이 해시거나 「첨부파일」뿐인 53곳, 주소가 죽은 7곳.
   죽은 7곳은 **자치단체 홈페이지에서 손으로 찾아** 주소를 고쳐 주면 된다(`data/disclosure.json`).
   나머지는 목록 화면 안으로 한 번 더 들어가야 잡힌다(공시 글이 아니라 **게시판 목록**을 가리키는 곳이 있다).
2. **「몇 번 눌러야 닿나」를 잰다.** ② 켜의 남은 칸이다. 자치단체 첫 화면에서 재정공시까지
   몇 번 눌러야 하는지 — 지금은 지방재정365가 준 **직통 주소**로만 재고 있어 실제보다 후하다.
3. **업체 이름을 다듬는다.** 지금은 글자가 같아야 한 곳으로 묶인다 —
   「(주)○○」와 「주식회사 ○○」가 갈라진다. 사업자번호가 자료에 없어 이름으로만 묶는 중이다.
   ⚠️ 함부로 합치면 **다른 업체를 한 곳으로 만든다.** 합칠 규칙을 정하기 전에는 그대로 둔다.
   ⭐ **2026-09-22 에 가른 것 — 두 가지가 섞여 있었다.**
   - 지방재정365 계약현황(`WCEGCF`)에는 **사업자번호가 진짜로 없다**(출력 13칸 전수 확인).
     여기서는 못 가져오는 것이 맞다.
   - 나라장터(조달청) 계약정보에는 **있다.** ✔ **2026-09-22 에 실제로 받아 봤다** —
     `corpList` 칸 맨 끝이 사업자등록번호다(위 2026-09-22 칸 참고). 이제 묶을 열쇠가 생겼다.
     아직 **받아만 봤고 모아 두지는 않았다** — 전수로 긁어 곳별로 붙이는 건 남은 일이다.
   - ⚠️ 키를 넣어도 **전부는 안 맞는다.** 나라장터는 조달청을 거친 계약만이라
     자치단체 자체 계약·수의계약은 빠진다. **번호가 붙는 만큼만 묶고 나머지는 이름 그대로 둔다.**
4. 보탬e 지방보조금 2025년치 전수 — ⑥ 판의 「보조금 단체」 칸이 아직 비어 있다.
   ✔ **개인이 받은 것도 일단 그대로 띄운다**(2026-09-22 사용자 결정, 위 「정해 주셔야 할 것」 5번).
5. **다년계약은 나중에 한다** (2026-09-22 사용자 결정 — "계약은 일단 세놔. 이건 나중에 하자").
   지금처럼 계약명 글자로만 세어 둔다(「2025년~2027년」·「1년차」).
   총액 줄과 연차 줄을 짝지으면 「그 해에 실제로 맺은 돈」이 나오지만,
   ⚠️ 짝짓기를 잘못하면 진짜 계약이 사라진다. **규칙을 세우기 전에는 세기만 한다.**

6. ⭐ **CLIK 목록 전수 받기가 도는 중이다 — 22일짜리다.** 하루 1,000회 한도가 박혀 있어
   `python scripts/fetch_clik.py` 를 **날마다 한 번씩** 돌려야 끝난다. 이어받기가 되니
   끊겨도 다시 부르면 된다. 무엇을 화면에 올릴지는 **사용자가 정한다**(아직 안 정했다).

⛔ **도메인 연결은 사용자가 말할 때까지 하지 않는다.**
